# NeuralHydrology GPU版本依赖包
# 适用于有NVIDIA GPU的用户

# 核心科学计算包
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
xarray>=0.20.0

# 深度学习框架 (CUDA 11.8版本)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 数据处理和可视化
matplotlib>=3.5.0
h5py>=3.6.0
netcdf4>=1.6.0

# 配置和工具
ruamel.yaml>=0.17.0
tqdm>=4.64.0
tensorboard>=2.8.0

# 性能优化
numba>=0.56.0

# 安装说明：
# 1. 确保已安装NVIDIA驱动和CUDA 11.8
# 2. 使用以下命令安装GPU版本的PyTorch：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. 然后安装其他依赖：
#    pip install -r requirements-gpu.txt
