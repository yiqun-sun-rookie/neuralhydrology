# =============================================================================
# WinSCP 同步脚本 - NeuralHydrology HPC
# 用法: 在 WinSCP 中执行此脚本，或通过命令行调用
# 更新: 2026-01-31 - 任务隔离同步（仅 Caravan 01）
# =============================================================================

# 打开连接（使用已保存的会话名称，避免明文密码）
open hhu_hpc

# 切换到项目目录
cd /data1/home/sunyiq/neuralhydrology
lcd "G:\github\pycharm\projects\neuralhydrology"

# =============================================================================
# Caravan Global 项目同步
# =============================================================================

# 1. 同步 src/caravan_global 目录（代码、配置、脚本、隔离 basin 列表）
synchronize remote -mirror -delete "src/caravan_global" "/data1/home/sunyiq/neuralhydrology/src/caravan_global" -filemask="|*.pyc;__pycache__/"

# =============================================================================
# 可选：仅同步当前任务文档（不影响其他任务）
# =============================================================================

put "draft/ideas/01_caravan_global.md" "/data1/home/sunyiq/neuralhydrology/draft/ideas/01_caravan_global.md"
put "draft/RESEARCH_INDEX.md" "/data1/home/sunyiq/neuralhydrology/draft/RESEARCH_INDEX.md"

# =============================================================================
# 设置权限
# =============================================================================
call chmod +x hpc/*.sh
call chmod +x src/caravan_global/hpc/*.slurm

# 关闭连接
close
exit
