# 持久进度

## 目标对照

- 当前目标：完成统一自动研究系统第二里程碑的受限候选运行环境，并在双重独立审核通过后准入。
- 禁止范围：不运行最终评估，不执行 64 或 531 流域正式搜索，不宣称超过基线。

## 当前结论

- 选择独立编排层方案，全部新增实现限定在 `src/unified_autoresearch/`。
- 基线检查：`python -m pytest src/fair_benchmark/tests -q`，66 项通过，0 项失败，4 条既有警告。
- 当前主工作区存在大量修改；实现已隔离到分支 `codex/unified-autoresearch-vertical` 的独立工作区。

## 证据

- 设计：`src/unified_autoresearch/docs/design.md`
- 实施计划：`src/unified_autoresearch/docs/implementation_plan.md`

## 审核判定

- 里程碑零第一轮独立审核失败：选择器缺少27个允许静态属性名的精确白名单。
- 第二个独立任务定向重跑后判为“已确认”：`p_mean` 改名为 `q_mean` 或 `model_nse` 时旧选择器均返回8个流域。
- 已按测试先行修复；新增回归测试先出现2项失败，修复后里程碑零测试15项全部通过。
- 两个内置子任务和一次临时命令行审核未能返回，未作为通过证据；有效审核来自两个独立 Codex 应用任务。
- 修复后独立复核通过：15项测试、18个文件数字指纹、两种禁止列反例和原八流域重跑均通过。
- 第二个修复后定向任务判定“已确认”：替换列绕过已被阻断。里程碑零完成。
- 里程碑一首版实现完成：候选、实验、状态和数字指纹采用单一数据库散列链；状态转换使用立即写事务；调度锁记录进程编号、创建时间、程序路径、运行标识和随机令牌；数字指纹覆盖8类组成。
- 里程碑一首版正常路径为27项测试通过、登记8条记录且0项校验失败，首版根散列为 `70589de0d002f84b07d44603ce20ee43fac47b54fb85a05301c33788895c5ea2`。
- 第一轮独立审核与第二轮定向复验均确认5项阻断：无锁状态启动、活锁替换竞争、链尾删除漏检、未列产物漏检、内部不一致指纹漏检。
- 五项修复均先出现对应失败测试：首批2项失败、次批3项失败。修复后完整新框架测试32项通过、0项失败，用时8.27秒。
- 修复内容：执行相关状态转换必须持有登记指定的真实调度锁；每条数据库记录同时生成排他创建的外部凭据；锁恢复由操作系统文件锁串行并在归档后复核被移动内容；产物输入必须是目录根且递归覆盖；完整性校验重新验证每条数字指纹的内部语义。

## 下一步

- 里程碑一第一修复证据根为
  `4b37af50abb0a1d128dc75b6125faea86da0e2ce5fbc3f49ae5fd9fe2dd707ce`；登记、完整指纹、34 项测试和
  五类攻击回归均通过，但完整独立审核发现只读快照清单在 Windows 长路径下遗漏 8 个实际存在的外部登记凭据。
- 第二个全新任务从原始字节确认：8 个凭据绝对路径均为 272 字符，复制使用长路径前缀而清单枚举没有，
  因此第一修复版判定失败并永久保留，不得覆盖。
- 下一轮先用长路径安全的物理文件集合断言产生失败测试，再修复快照枚举；在新的
  `runs/unified_autoresearch/milestone1_repair2/` 重新生成证据和只读快照，并重新执行两次独立审核。
- 第二修复证据根为
  `4cb937ffe680f249a27f9c63756f330538fef11b87cd14806faca348b9ad3f98`；快照长路径清单、登记、指纹和
  35 项测试通过，第一次完整独立审核判定通过。
- 第二次定向复验发现运行包清单生成仍使用普通递归枚举；第三个全新任务从深快照原始文件集合确认，
  该入口会漏掉 8 份 272 字符路径凭据。第二修复版因此未达到双通过门槛并永久保留。
- 下一轮在深运行根为 `PACKAGE_MANIFEST.sha256` 写失败闭包测试，修复后使用新的
  `runs/unified_autoresearch/milestone1_repair3/` 生成证据并重新双审核。
- 第三修复证据根为
  `5ad8264fb7abbb6be0aacc14f7eb20462b2f5e9d64870ec66cbc4a2f0bdb2969`。快照预审核在较长系统临时根下
  出现 35 项通过、1 项失败：测试自身用普通路径读取已成功生成的深路径运行包清单。
- 第三修复版未进入独立审核并永久保留；测试读取改为独立长路径安全接口后，相同类型长临时根下 36 项通过。
  下一次生成使用新的 `runs/unified_autoresearch/milestone1_repair4/`。
- 第四修复证据根为
  `945714627daf54dc315ddb37dd0450905254c9c9d526211f694024b7bee71074`。长路径安全自检和两个全新独立任务均确认：
  快照 67/67、运行包 22/22，登记/凭据 8/8，完整测试 36/36，七项定向回归 7/7，0 项阻断。
- 里程碑一第四修复版达到双通过门槛；准许开始受限候选运行环境的测试先行实现。
  该结论不授权最终评估、公平基准评分、正式流域搜索或任何超过基线的表述。
- 两次审核之后的主任务最终复核遗漏 `PYTHONDONTWRITEBYTECODE=1`，在第四修复版快照新增 4 个字节码缓存文件。
  Windows 目录只读属性没有阻止新文件创建；当前快照比清单多 4 项，准入随即撤销。
- 污染快照不清理、不覆盖。下一轮先以“封存快照拒绝创建新文件”的失败测试验证操作系统级写保护，
  再使用新的 `runs/unified_autoresearch/milestone1_repair5/` 生成证据并重新双审核。
- 第五修复证据根为
  `1f751b1f922db3604a39e42c57b477b5047d5365d355f3248623e15d0f5d6469`。操作系统在根目录和深层目录均实际拒绝新文件，
  完整测试后快照仍为物理/清单 72/71，运行包为 23/22，访问控制描述符和内容摘要不变。
- 两个没有实现对话记录的全新任务分别完成完整审核和定向复验，均判定 `PASS`；登记/凭据 8/8，
  完整测试 36/36，未发现新阻断。
- 第五修复版恢复并最终满足准入门槛：准许开始受限候选运行环境的测试先行实现；
  最终评估、公平基准评分、正式流域搜索和超过基线声明仍被禁止。
- 里程碑二第三修复证据根为
  `6c90c4c8fa2d3b1e16e59c911239fd1fdf5c22f342a088a5baaaa7e3c34c83ca`；快照 90 个物理文件、89 项清单，
  运行包 23 个物理文件、22 项清单，登记和外部凭据均为 8 项，完整测试 88 项通过、1 项跳过。
- 两个没有实现历史的有效独立任务均判定第三修复版失败。第二层从原始合成案例确认五项阻断：无效或额外预测产物被接受、
  处理器资源声明未执行、依赖版本未核对、候选运行未接入登记链、Windows 深路径凭据创建失败。
- 第三修复版永久保留且不得覆盖。下一轮只处理预测产物契约：要求常规文件、有效 Parquet 格式、`basin/date/qsim` 列，
  且输出根不含未声明的顶层产物；其余已确认问题继续保持未准入状态，后续逐轮处理。
- 里程碑二第四修复证据根为
  `0a2056ac7d458291534b6ba9a18f1b5be8f13546049ec1b9680a03b593378342`；完整测试 92 项通过、1 项跳过，
  快照 93/92、运行包 23/22，登记和凭据 8/8，写入探针在根和深层均被操作系统拒绝。
- 两个全新有效任务均确认预测 Parquet、必需列和额外顶层输出规则已经修复，但都判定第四修复版失败。
  第二层再次确认六项剩余阻断，包括同目录辅助模块无法导入、依赖与处理器声明未执行、检查点保留数未执行、
  候选运行未登记和 Windows 深路径凭据失败。
- 第四修复版永久保留且不得覆盖。下一轮只修复普通多文件候选：入口脚本必须能导入候选根内的同目录模块，
  同时保持未声明外部模块的导入限制；其他阻断继续逐轮处理。
- 里程碑二第五修复证据根为
  `bbc7bc9930fa98a1d6d43491ed560235612155a572b98a9cdfad8a310a0d3c8f`；完整测试 93 项通过、1 项跳过，
  快照 95/94、运行包 23/22，登记和凭据 8/8，根与深层写入探针均被拒绝。
- 两个有效全新任务均确认同目录辅助模块导入和预测 Parquet 规则已经修复，但都判定第五修复版失败。
  第二层从原始证据确认依赖精确版本、处理器请求、检查点保留数和实际候选运行登记仍未实现。
- 深路径项在本轮出现范围不一致：首个审核的 296 字符案例失败，第二层只检查了现有 280 字符路径并称其通过；
  该较窄检查不能否定更深案例，本轮失败结论也不依赖此项。
- 第五修复版永久保留且不得覆盖。下一轮只修复候选依赖版本准入：启动前必须把每个 `name==version` 声明
  与实际环境中的唯一安装版本精确核对；其余阻断继续逐轮处理。
- 里程碑二第六修复证据根为
  `c79d25a37c93f5fd6be9838dcf190a9dcc1dbf6aece6fdf17cac282f1b21d3a6`；完整测试 96 项通过、1 项跳过，
  快照 98/97、运行包 23/22，登记和凭据 8/8，根与深层写入探针均被拒绝。
- 两个有效全新任务均确认依赖精确版本预检已经修复：错误版本和缺失包在启动前拒绝，唯一正确版本成功并保存预检证据。
  两者也均确认处理器请求、检查点保留数、实际候选运行登记和 Windows 深路径凭据仍不满足。
- 第六修复版永久保留且不得覆盖。下一轮只处理任务声明的中央处理器和图形处理器容量：按本次任务请求与当前可用容量比较，
  不设置统一固定门槛；内存仍按任务估算和安全余量判断。
- 里程碑二第七修复证据根为
  `a04f26753b20d5b000c95f7a8690c06e66b8c4256a56d557c6da298afa2ed5f2`；完整测试 98 项通过、1 项跳过，
  快照 100/99、运行包 23/22，登记和凭据 8/8，根与深层写入探针均被拒绝。
- 两个有效全新任务均确认任务级中央处理器、图形处理器、内存和磁盘容量预检已经修复，并确认检查点保留声明与实际行为不一致、
  实际候选运行仍未登记。
- 第七修复版永久保留且不得覆盖。下一轮只解决检查点保留契约；鉴于目标要求永久保留失败与历史证据，不删除旧检查点，
  而是把当前描述契约改为保留全部不可覆盖版本并以测试固定该行为。
- 里程碑二第八修复证据根为
  `0e3573e4295627d75dc5949742e1338de81fe5a5b72ee08f34d2a3b0233ea81e`；完整测试 100 项通过、1 项跳过，
  快照 102/101、运行包 23/22，登记和凭据 8/8，根与深层写入探针均被拒绝。快照内完整重跑后文件集合和散列不变。
- 两个全新独立任务均确认检查点契约已经修复：描述必须为保留全部，旧的保留两个声明被拒绝，三个不可覆盖版本均保留且可复验。
  两者也均从新合成案例确认两个剩余阻断：真实候选执行没有进入登记链或生成覆盖真实输出和日志的最终指纹；
  登记模块无法在 281 字符 Windows 路径直接创建外部凭据。
- 第八修复版永久保留且不得覆盖。下一轮先修复登记模块的 Windows 深路径凭据创建和复验，因为真实候选执行接入登记链
  依赖该基础能力；随后再单独处理候选执行、真实调度锁、状态链和最终指纹的一体化登记。
- 里程碑二第九修复证据根为
  `17d8bd76126fe0ecb975c8340afef1013f498c015ca587e1f7726d7a51a96731`；完整测试 101 项通过、1 项跳过，
  快照 104/103、运行包 23/22，登记和凭据 8/8，快照内完整重跑后文件集合和散列不变。
- 两个全新独立任务均确认 Windows 深路径凭据修复成立。独立案例中的数据库路径为 241 至 246 字符、
  凭据路径为 293 至 298 字符，均直接追加 1 条记录、创建并读取 1 份凭据，完整验证为 0 项失败；普通路径对照也通过。
- 两者均确认唯一剩余准入阻断：真实候选执行仍不接收登记、实验标识、运行标识或调度锁，成功运行产生的真实输出和日志
  没有候选、实验、状态和最终指纹记录。第九修复版永久保留且不得覆盖。
- 下一轮只实现统一候选运行编排入口：在真实调度锁下追加候选、实验及完整状态链，并在运行结束后追加覆盖该次真实输出
  和全部日志的八部分最终数字指纹；随后使用新的证据根和两个全新上下文复核。
- 里程碑二第十修复证据根为
  `367edaf567049707152fb7e4b0e21638ea2b8161140a72e327ac082be6b276ae`；冻结测试 108 项通过、1 项跳过，
  快照 134/133、运行包 23/22，外层登记和凭据 8/8，快照内完整重跑后文件集合和散列不变。
- 第十修复版额外封存一个真实已登记合成候选探针。其内部根散列为
  `ca6d0572ae14c186f0802b53f094f07e98521fb5594d79cfadaa47879f5d7385`，共有 26 个物理文件、8 条登记、
  8 份凭据、5 个状态、1 个最终指纹、1 个输出和 7 个日志；调度锁已释放，全部真实输出和日志均由内部指纹覆盖，
  外层指纹又逐字节封存全部 26 个文件。
- 第一个无实现历史的全新命令行上下文完成完整审核并判定 `PASS`；第二个全新命令行上下文从原始字节和 15 项定向测试
  独立复验并判定 `CONFIRMED_PASS`。两者均确认成功、两类失败、检查点、启动前拒绝和恢复路径已登记并生成最终指纹，
  Windows 深路径凭据、检查点保留、精确依赖和任务级资源预检无回退。
- 第十修复版满足双通过门槛，统一自动研究系统第二里程碑受限候选运行环境正式准入。准入不授权最终评估、
  公平基准评分、64 或 531 流域正式搜索或超过基线声明；真实候选必须使用统一已登记编排入口。
- 里程碑三第一轮只实现并审核开发数据边界，未使用真实水文数据。证据根为
  `7c053981660b84a6f7eabb4ff3d7bdd096fc365530dcba672e65138bd3886d79`；冻结测试118项通过、1项跳过，
  快照113/112、运行包23/22，登记和凭据8/8，清单、数字指纹及只读快照均完整。
- 两个全新独立上下文分别判定 `FAIL` 和 `CONFIRMED_FAIL`：当特征表和目标表同一行日期同时为空时，
  日期最小值、最大值和匹配键检查均未拒绝，后续区间筛选把该行静默丢弃。合成输入每表48行，forward输出
  31条训练与16条验证，reverse输出32条训练与15条验证，均少1行。第一轮证据永久保留且不得覆盖。
- 第二轮把“空日期及非午夜时间戳必须在创建输出根之前拒绝”固定为失败测试；对应反例先失败，最小修复后
  数据边界定向测试12项通过。第二轮仍只使用合成有限数值，等待新证据根和两次全新独立复核。
- 里程碑三第二轮证据根为
  `5820b07e167e6c2647e8d1de20b518d79de0013b5fec0a4ca5afc5a2c216c0cf`；冻结测试120项通过、1项跳过，
  快照115/114、运行包23/22，登记和凭据8/8。两个全新独立上下文分别判定 `PASS` 与
  `CONFIRMED_PASS`，并从48键合成源确认每套协议32个训练键、16个验证键、交集0、并集48；空日期、
  非午夜时间戳及其余关键反例均在最终输出根创建前拒绝。
- 数据准备边界达到本阶段双通过门槛。该结论只准许继续实现外部开发验证评分，不授权真实数据读取、
  封存最终评估、公平评分、正式流域搜索或超过基线声明。
- 里程碑三第三轮外部开发评分证据根为
  `148ac7210c0ba8e92a37e029bbd4f7b8ef637706b88a08c7bbbdf0e86d06f286`；冻结测试131项通过、1项跳过，
  清单、八部分数字指纹、登记链和快照不变性均成立。
- 两个全新独立上下文分别判定 `FAIL` 与 `CONFIRMED_FAIL`。已确认阻断为：数据包清单能自我授权修改；
  评分报告不绑定候选、类别和登记运行；两个协议报告不要求来自同一冻结数据包；有限的极端负指标求平均会溢出，
  序列化失败后留下部分输出。第三轮证据永久保留且不得覆盖。
- 下一轮只修复外部可信清单散列、候选与登记运行身份、双协议同源和有限平均值的原子写入；所有修复必须先有失败测试。
- 第四轮对应失败测试均先出现预期失败：清单字节与外部冻结散列不一致、报告候选/类别/运行/数据包身份漂移、
  额外或缺失字段，以及两个有限 `-1e308` 指标直接相加溢出。修复后评分定向测试19项通过。
- 修复内容：评分前核对外部冻结的数据包清单原始字节散列；评分报告绑定候选、类别和已登记运行；汇总器要求
  精确字段、两个给定运行及同一数据包散列；平均值改为分别除以二后相加；评分和汇总均在内存完成有限序列化后
  才排他创建结果文件。第四轮等待新不可覆盖证据与两次全新独立复核。
- 里程碑三第四轮外部开发评分证据根为
  `8a5cc937a4b211f63294057959a77239d214ce859da97bd0800085ef1798d696`；冻结测试139项通过、1项跳过，
  快照122/121、运行包23/22，登记和外部凭据8/8。
- 两个没有实现对话记录的有效全新独立上下文分别判定 `PASS` 与 `CONFIRMED_PASS`。
  外部冻结清单散列、候选/类别/登记运行身份、双协议同源、极端有限值平均和失败时不留部分输出均已从合成反例直接确认。
- 外部开发验证评分达到本阶段双通过门槛；只准许继续四类轻量参考候选的测试先行实现。
  本结论不授权读取真实水文数据、封存最终评估、公平基准评分、正式流域搜索或超过基线声明。
- 里程碑三第五轮四类参考候选证据根为
  `4a1f980b61b94d75696995f098d7ab3d95fa51668288a0557c47ae87f49065a9`；冻结测试 149 项通过、1 项跳过，
  快照 133/132、运行包 23/22，登记和外部凭据 8/8。第一层独立功能审核判定 `PASS`，但第二层原始复验判定
  `REFUTED`：`runtime/bootstrap.py` 在访问记录和运行限制启用前导入声明依赖。良性合成依赖成功读取未授权文本并把内容
  传给候选，但 11 条访问记录中没有该文本、依赖源码或依赖导入事件。第五轮永久保留且不得覆盖，不能准入。
- 第六轮只修复依赖初始化顺序。对应回归测试先复现旧行为：位于声明读取根之外的合成依赖及其文本被读取，子进程返回 0；
  修复后访问记录先启动，依赖源码读取被记录并拒绝，定向契约测试 6 项和四类参考候选测试 8 项通过。
- 修复后的完整测试为 150 项通过、1 项因当前 Windows 账户不能创建符号链接而跳过、0 项失败。依赖初始化期间仅允许并记录
  当前 NumPy、pandas 和 PyArrow 初始化所需的固定 Windows 系统库事件；依赖初始化结束后，子进程、网络和动态本地库限制
  仍由原定向回归确认。第六轮等待新的不可覆盖证据根和两个全新独立上下文复核。
- 第六轮证据根为 `184dd693501710c739cd80b2d4e4028efa21a395ae2d3cd0ce0a5c61c89d9d02`；冻结测试
  150 项通过、1 项跳过，快照 135/134、运行包 23/22，登记和凭据 8/8。两个全新独立上下文分别判定 `FAIL` 和
  `CONFIRMED_FAIL`。
- 已确认的第一项阻断是快照长路径：概念降雨径流实现的绝对路径为 268 个字符，普通 `Path` 和 `shutil.copy2()` 无法读取，
  从该快照直接运行出现 2 项失败；Windows 扩展长度路径可读同一 2907 字节且散列与指纹一致。第六轮永久保留且不得覆盖。
- 已确认的第二项阻断是依赖源码边界：第六轮回归只证明读取根之外的依赖源码会被拒绝，而当前标准库根覆盖全部
  `site-packages`，不能证明只允许声明依赖的源码。下一轮按单问题原则先修复会阻断全部快照复验的候选物化长路径；
  依赖源码边界留到随后独立一轮。
- 第七轮长路径复制回归先因缺少长路径安全复制入口而失败；修复将 Windows 本地路径和网络共享路径转换为扩展长度路径后再复制，
  不改变候选方法。270 字符以上合成源复制测试及四类物化测试共 5 项通过，四类候选完整物化、训练和预测测试 9 项通过。
  第七轮等待新的不可覆盖证据与两次独立复核；本轮即使确认长路径修复，也不消除已记录的依赖源码边界阻断。
- 第七轮证据根为 `fe68d07c9dab5ea22443cf17294f0523bd9f833cd83538554e6eeb36fda75290`；冻结测试
  151 项通过、1 项跳过，快照 137/136、运行包 23/22，登记和凭据 8/8。两个全新独立上下文分别判定长路径修复
  `PASS` 与 `CONFIRMED_PASS`：268 字符冻结源可物化，四类复制字节一致，候选测试 9 项通过，8 个训练/预测运行的
  登记、凭据和输出日志指纹均闭合。
- 长路径问题完成；总体候选准入继续为 `HOLD`。下一轮只处理依赖源码范围：标准库读取根不能继续隐式授权整个
  `site-packages`，应只允许启动前记录的声明依赖及其实际初始化闭包，同时保持标准库和四类候选正常运行。
- 第八轮依赖范围反例先复现旧行为：合成解释器根包含 `site-packages`，声明依赖从其自身源码外读取未声明良性文本，
  子进程仍返回 0。修复后同一文本读取被记录并拒绝；声明依赖源码目录本身保持可读。
- 修复在启动前独立解释器中导入精确声明依赖，记录实际初始化闭包；当前 NumPy、pandas 和 PyArrow 闭包为 12 个精确路径。
  运行策略继续允许标准库，但把 `purelib` 和 `platlib` 标为受限根，只允许该闭包；不再因标准库父目录而授权整个
  `site-packages`。依赖范围和活动版本定向测试 3 项、四类候选物化/训练/预测测试 9 项均通过，等待第八轮不可覆盖证据。
- 第八轮证据根为 `d5e6f610ba3bf6d930aeee295ad669d108336ce9c8defb16d2d0f40b9ea8ecba`；冻结测试
  151 项通过、1 项跳过，快照 139/138、运行包 23/22，登记和凭据 8/8。
- 两个全新独立上下文分别判定 `PASS` 与 `CONFIRMED_PASS`：12 个初始化闭包路径不包含整个 `site-packages`，
  461 次真实读取闭包外为 0；同级未声明文本和初始化后的四类禁止能力均被记录并拒绝；四类候选 8 次训练/预测运行、
  长路径、有限 Parquet、登记、凭据及最终指纹无回退。
- 四类轻量参考候选阶段达到双通过门槛。只准许继续实现合成开发数据上的中断恢复、干净重跑、双协议外部评分和
  32 个有限开发单元；不授权真实水文数据读取、最终评估、公平基准评分、正式流域搜索或超过基线声明。
- 第九轮只处理训练中断恢复与干净重跑一致性。回归测试先因恢复工作流不存在而失败；实现后，自定义 Python 参考候选的
  forward 训练先在已登记运行中按停止请求发布不可覆盖检查点，再由新的已登记运行挂载并恢复，同时第三个已登记运行从头训练。
  三个运行各有独立登记链和最终指纹；恢复模型与干净模型原始字节完全相同。定向端到端测试 1 项通过，等待保存实际合成产物。
- 已保存一套只含 1999-10-01 至 2008-09-30 的八流域合成源、双协议数据包和真实恢复探针。检查点根散列为
  `06a4c8e0684a906d3288c34edc0eac1be5da7a4d98ea2b0a5e86b9630adfeed6`；恢复与干净模型散列均为
  `7bb0dca0335bcf387f46799f6d9e8b6d4ce0345a87ca7708797d0a2f47cb2e64`。三个实际运行状态依次为
  `checkpointed`、`succeeded`、`succeeded`，等待第九轮外层不可覆盖证据与双重独立复核。
- 第九轮外层证据根为 `271c46ebbd9fdd9c851ae2416c6a3bf0af348a4d02deee3f7cffdc4cce8389e2`；冻结测试
  152 项通过、1 项跳过，快照 253/252、运行包 23/22，外层登记和凭据 8/8。两个全新独立上下文分别判定
  `FAIL` 与 `CONFIRMED_FAIL`，第九轮证据永久保留且不得覆盖。
- 第一项独立阻断是 Windows 260 字符边界：冻结源码在全新临时 Git 仓库重跑时，实际存在的
  `CHECKPOINT_VERIFIED.json` 恰为 260 字符，普通 `Path.is_file()` 返回假；扩展长度路径读取和复验同一文件成功。
- 第二项独立阻断是历史运行字节不闭合：三个运行指纹各登记 117 个全局未跟踪文件，其中
  `src/unified_autoresearch/evidence/PROGRESS.md` 的历史字节在外层快照建立前已更新；快照只能重放 116 项，且不存在
  历史散列对应字节。外层根本身完整，但不能补回三个内层指纹引用的缺失原始字节。
- 下一轮修复检查点全部读写的 Windows 扩展长度路径，并让每个已登记运行排他保存当时的 Git 差异与状态原始字节；
  内层指纹只登记显式代码、配置、依赖和数据输入范围内的未跟踪文件，不再引用无关的全局进度文档。两个反例均已先失败，
  修复后的检查点边界、登记运行、指纹和恢复定向测试 18 项通过；等待新的不可覆盖实际产物和双重独立复核。
- 第十轮恢复修复证据根为 `3d9797474126ab8a334dcb1e28721e3bfb32315ecdc310a3d6936cefc3ed1615`；冻结测试
  153 项通过、1 项跳过，外层根、Git 原始字节、清单和外层登记均内部一致。两个全新独立上下文仍分别判定
  `FAIL` 与 `CONFIRMED_FAIL`，第十轮证据永久保留且不得覆盖。
- 两层复验均确认数字指纹递归仍使用普通 Windows 路径。原运行的发布检查点目录为 245 字符，其中模型文件 256 字符被收录，
  但请求和验证文件为 269、270 字符而被静默遗漏；中断运行的 8 份深路径外部凭据也全部没有进入外层快照。
- 全新 Git 仓库中，当发布检查点目录达到 272 至 274 字符时，恢复端到端测试稳定失败于
  `registry/fingerprint.py::_manifest()` 的普通 `Path.rglob()`。恢复挂载的完整三文件证明检查点本身已正确发布，缺陷在指纹枚举。
- 第十一轮把超过 260 字符的指纹产物、恢复汇总和外层快照闭包分别固定为三个失败测试；修复后使用扩展长度路径进行物理枚举、
  文件大小和流式散列。三个反例通过，129 至 130 字符基础临时目录下的完整恢复端到端测试通过，相关 26 项测试通过；
  等待新的不可覆盖实际产物和两次独立复核。
- 第十一轮恢复修复证据根为 `bfc9d799e008bfcb35dbffb011b3e0a6a8c82a4b1ace8a6849d8a76230f5abd1`；冻结测试
  155 项通过、1 项跳过。两个完全独立的有效上下文分别判定 `PASS` 与 `CONFIRMED_PASS`。
- 两层均确认三个运行各有 8 条登记、8 份凭据和 9 个日志，恢复汇总 97 项与 97 个物理文件一致，发布与挂载检查点三文件
  逐字节相同。全新 Git 仓库的恢复端到端测试在 297 至 299 字符检查点路径下通过，最长归档路径为 362 字符；
  精确 260 字符和不可覆盖行为也通过。
- 训练中断恢复、干净重跑一致性及内外层证据闭包达到双通过门槛。下一轮准许把四类候选分别在 forward 与 reverse
  开发协议中完成已登记训练、预测和外部评分，目标是恰好生成 32 个有限开发单元；仍不授权真实水文数据、最终评估、
  公平基准评分、64 或 531 流域正式搜索或超过基线声明。
- 第十二轮完整开发循环先以缺少统一入口为失败测试；实现后按固定顺序运行四类参考候选。每类在 forward 和 reverse
  协议中分别完成一次已登记训练和一次已登记预测，共 16 个运行；每个预测由外部开发评分器生成 8 个流域指标，
  两协议再合并为每类 8 个有限单元，总计恰好 32 个唯一候选类别—流域单元。
- 合成端到端循环测试为 1 项通过，用时 200.83 秒；逐项确认 16 个运行各有完整 8 条登记和 8 份凭据、8 份评分报告
  身份绑定各自预测运行、32 个指标全部有限且重复目标拒绝覆盖。等待新的实际循环产物、外层证据和双重独立复核。
- 已在新的不可覆盖根保存第十二轮实际合成开发循环：4 类候选、2 套协议、16 个已登记运行、8 份外部评分报告和
  32 个唯一有限单元；循环汇总建立前的产物清单为 492 项。运行时可用内存为 2.99 吉字节，候选串行执行，
  每次声明的峰值内存加安全余量为 0.60 吉字节，未启用并行且没有内存崩溃风险。等待外层证据和双重独立复核。
- 第十二轮完整开发循环外层证据根为 `27273db6b786e48356cb2fc72adb0aaee37971a6a51951b35e58d76263fb905a`；
  冻结测试 156 项通过、1 项跳过，外层登记和凭据 8/8。两个完全独立上下文分别判定 `PASS` 与 `CONFIRMED_PASS`。
- 两层均确认 16 个运行各有 8 条登记、8 份凭据和 9 个日志，合计 19,512 条候选访问事件且拒绝和禁止路径命中为 0；
  493 个循环物理文件闭合为 492 项预汇总清单加 1 份循环汇总，最长绝对路径 364 字符，没有遗漏。
- 8 份外部评分报告的候选、类别、协议、实际预测运行、预测、开发真值和数据包散列均一致。第二层从原始预测和开发真值
  重新计算全部 64 个协议—流域纳什效率和 32 组正向、反向、均值，保存值最大绝对差为 0，所有 32 个单元有限且唯一。
- 全新 Git 仓库完整重跑再次得到 4 类、2 协议、16 运行、8 评分和 32 单元；重复输出根被拒绝且原字节不变。
- 第三里程碑完成并达到双重独立准入门槛：开发数据边界、外部评分、四类候选、训练中断恢复、干净重跑、深路径证据闭包和
  完整 32 单元开发循环均有不可覆盖证据。结论只授权继续在同一受限开发环境内提出新的候选；不授权真实水文数据读取、
  1989-10-01 至 1999-09-30 最终评估、公平基准评分、64 或 531 流域正式搜索或任何超过基线声明。
- 台阶 A 完成：真实开发专用数据根、真实两套开发数据包和真实数据上的两次连续完整纵向运行均已产生不可覆盖证据，
  32 个单元全部有限、禁止路径命中 0、两次运行评分与汇总逐字节一致。同时修复既往审核因软链接不可用而漏检的
  三处运行布局逃逸和两处评分器失效检查，冻结测试由 156 通过 1 跳过变为 170 通过 0 跳过 0 失败。
  详见 `evidence/REAL_DATA_STEP_A_20260724.md`。里程碑四、规模扩展、比较器强化和双重独立复核仍未完成。
- 里程碑四资源监督流程走通（缩减范围，未完成）：独立监督进程先于候选启动，四项归属字段核验通过才动手，
  按任务间隔采样并原子追加资源日志，越限写出冻结格式受控停止请求；采样间隔经登记运行透传。
  真实数据上跑通 1 类候选 × 1 套协议的训练、预测、评分，训练 15 条、预测 20 条采样，
  实测进程峰值内存 0.117 与 0.099 吉字节，对照声明值 0.35 加余量 0.25 吉字节，声明保守约三倍。
  冻结测试 180 项通过、0 跳过、0 失败。仍缺：峰值估算方法冻结、两次连续完整运行、
  64 与 531 流域扩展剩余清单、双重独立复核、真实训练规模重任务、汇总间隔实现。
  详见 `evidence/MILESTONE4_WALKTHROUGH_20260724.md`。
- 里程碑四监督下两次完整循环与停机策略标定（2026-07-25）：给 `run_full_development_loop` 加可选监督参数（测试先行，
  新监督端到端测试先红后绿，16 个运行各有已发布归属身份与连续采样）。开监督首轮连跑中，第二次在 `fusion/forward/train`
  被监督**首次真实触发受控停止**：系统内存连续两次跌破旧的绝对 4 吉字节下限（4.02→3.953→3.727），而候选自身仅 26 兆字节，
  属外部压力误停。根因为 `resource_safety_v1` 绝对下限落在本机正常波动带内。新增 `resource_safety_v3`（不改冻结的 v1）
  改为按任务相对判停：候选超己声明包络 1.5 倍、或系统可用内存低于 1.5 吉字节近-耗尽下限时才停；发布身份新增声明内存预算。
  测试先行补 v2 误停回归与候选超包络两测，更新受监督发布身份测试。v3 下重跑 `supervised_development_loop_v3_r1/_r2` 均完整：
  五项门槛全过、四灯全灭、各 16 运行全程受监督、0 受控停止、科学子集 12 文件逐字节一致；`milestone_four_run_gate_passed=true`。
  关键实证：v3_r2 系统可用内存最低 2.87 吉字节（低于旧 4.0 下限却未停），v3 在真实条件下修复 v2 失败。实测进程峰值
  ≈0.138 吉字节 vs 声明 0.60，保守约 4.36 倍。仍缺：64/531 扩展剩余清单、双重独立复核（须在能建软链接的环境跑）、
  真实重任务/长时/并行/图形处理器场景、汇总间隔、纳入版本控制。详见 `evidence/MILESTONE4_SUPERVISED_LOOPS_V3_20260725.md`。
- 里程碑四第 6 条双重独立复核完成，并按测试先行修掉三处审核弱点（2026-07-25）：两个互不通气的独立上下文分别判定
  **CONFIRMED_PASS**（原始字节九项自算全对上）与 **PASS（有保留）**（功能对抗式审核）。三处弱点均已修，不推翻里程碑事实：
  **F1** 未声明内存预算时候选自占用刹车默认跳过——补特征测试钉死其退化为"仅系统级监督"（系统近-耗尽下限仍生效）并注明；
  **F2** "四个安全灯"为硬编码自证循环、曾被当作已验证门槛——校验器改为从每个运行原始 `access.jsonl` 独立复算禁止访问数
  （`decision=="deny"`），运行门槛只依赖该独立复算值，自报四标记降级为"仅存档、不计入门槛"，schema 升 `v2`，新增
  `test_supervised_loop_verification.py` 钉死自报不进门槛；**F3** 发布监督身份未锁严格 JSON——无穷大预算改序列化为 `null`
  并加 `allow_nan=False`，新增严格 JSON 往返测试。对两个冻结 v3 产物重跑新校验器：两次独立复算禁止访问数均为 0、与自报一致，
  `milestone_four_run_gate_passed` 在更严独立判据下仍 `true`，结果落于新文件 `..._v3_r1/VERIFICATION_INDEPENDENT_GATE.json`
  （不覆盖冻结 v1）。冻结测试 **185 通过 / 5 跳过 / 0 失败**（5 跳过为软链接权限所限的逃逸测试，须在开发者模式/管理员环境补跑）。
  仍缺：64/531 扩展、真实重任务/长时/并行/图形处理器场景、汇总间隔、纳入版本控制。详见 `evidence/MILESTONE4_SUPERVISED_LOOPS_V3_20260725.md` §六。
