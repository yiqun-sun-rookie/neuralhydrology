# 第三里程碑第十轮恢复修复原始证据复验

结论：**确认失败**。

快照所列文件内部自洽，但遗漏了中断运行的全部外部凭据和发布检查点的两份验证文件。

## 结论性缺口

- 三个 SQLite 数据库均为 53,248 字节、完整性检查为 `ok`、各有 8 条有效散列链记录。中断运行只有 0/8 份外部凭据，独立 `Registry.verify()` 精确报告 8 个 `registry receipt missing`；恢复与干净运行各有 8/8 份凭据和 0 个失败。
- 发布检查点仅有 338 字节的 `model.json`，缺少 `CHECKPOINT_REQUEST.json` 和 `CHECKPOINT_VERIFIED.json`，检查点复验报 `checkpoint request is missing`。
- 恢复挂载完整包含三份文件，可独立重算并验证根散列 `06a4c8e0684a906d3288c34edc0eac1be5da7a4d98ea2b0a5e86b9630adfeed6`。

## 长路径根因

- 冻结源码 119 个文件逐字节复制到全新 Git 仓库；使用 132 字符基础临时目录后，发布检查点目录长 274 字符。
- 开发包清单 222 字符、训练输入 243 字符，仍可普通读取；扩展路径下发布检查点三文件完整且检查点复验成功。
- 完整恢复端到端测试稳定失败于 `registry/fingerprint.py:61` 的普通 `Path.rglob("*")`。
- 原运行等长探针中，发布目录为 245 字符：`model.json` 为 256 字符并被普通文件判断收录；请求和验证文件为 269、270 字符并被静默遗漏。
- 短路径恢复端到端对照通过；恰 260 字符的检查点验证与挂载对照也通过。缺陷位于数字指纹递归和文件判断，不在检查点验证与挂载本身。

## 其余原始证据

- 三个内层八部分指纹根均内部可重算：
  - 中断：`e0203834b132ef3bc06dfa6f4dfbba81226cdd73bd9fea85d692cab2eb0f50fe`
  - 恢复：`dda8fc217080e7888db7d61b42303f8e9324df50b4727928fbb2aee2e979c73f`
  - 干净：`526b9bf14433729b66eb4cfb7244439b872d3600efdecb8bef5ff3018a308773`
- 三个运行各有 9 个日志；保存的 Git 差异为 0 字节，状态为 8,015 字节，原始字节散列与内层指纹一致；未跟踪清单为空，没有引用进度文档。
- 恢复汇总含 87 项文件并与现存 87 项匹配，但没有列出已发布检查点的请求和验证文件。
- 外层八部分根重算为 `3d9797474126ab8a334dcb1e28721e3bfb32315ecdc310a3d6936cefc3ed1615`；359 项引用、235 个唯一文件均与其所列内容匹配。
- 外层登记有 8 条记录和 8 份凭据，0 个失败；运行包清单 22 项、快照清单 251 项均与所列文件匹配。
- 快照复验前后均为 252 个文件、1,942,275 字节，整树根保持 `de3742974e86c5357623bcd7bfcb3feeb40403326f013a07ae2d271f8c16c144`。
- 数据仅含 8 个冻结开发流域、Maurer 气象驱动及 1999-10-01 至 2008-09-30 开发区间。

## 保留探针

`G:\nh_round10_raw_probe_20260720_t2_3d979747`

- `fresh-source-repo`
- `long-basetemp-*`
- `short-basetemp`
- `exact260-validation-*`
- `exact260-mount-run-*`
- `fresh-source-repo\original-length-probe`

边界：未读取、寻找或评分最终评估期数据；未运行公平基准评分程序；未执行 64 或 531 流域正式搜索；未作超过基线声明。
