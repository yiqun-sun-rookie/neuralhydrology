# 第三里程碑第十轮恢复修复独立审核

结论：**失败**。

冻结快照内部散列自洽，但深路径恢复重跑失败，而且冻结证据遗漏中断运行的外部凭据和检查点验证文件。

## 阻断项

- 在两个不同的 130 字符基础临时目录中，恢复端到端测试稳定失败。检查点目录达到 272 字符后，`registry/fingerprint.py::_manifest()` 使用普通 `Path.rglob()`，产生 `FileNotFoundError`。
- 中断运行的 SQLite 数据库有完整 8 条记录，但冻结快照中没有该运行的 `registry/receipts` 目录，8 份外部凭据全部缺失；恢复运行和干净运行各有 8 条记录及 8 份凭据。
- 冻结快照内的已发布检查点只保留 `model.json`，缺少 `CHECKPOINT_REQUEST.json` 和 `CHECKPOINT_VERIFIED.json`。两份文件也没有进入中断运行指纹、外层指纹、恢复汇总清单或快照清单。
- 恢复运行挂载的检查点完整包含三份文件，且根散列可重算为 `06a4c8e0684a906d3288c34edc0eac1be5da7a4d98ea2b0a5e86b9630adfeed6`，证明冻结过程遗漏了原已发布检查点的关键证据。

## 已确认成立的部分

- 外层八部分根独立重算为 `3d9797474126ab8a334dcb1e28721e3bfb32315ecdc310a3d6936cefc3ed1615`。
- Git 提交为 `ffbde6d9091df08363ff5b7ab0492bdd02873c42`；原始差异 0 字节，状态 8,015 字节，保存值、工作树和指纹一致。
- 快照清单 251 项、运行包清单 22 项均与其实际列出内容一致；冻结测试为 153 项通过、1 项跳过、0 项失败。
- 三次运行状态依次为 `checkpointed/75`、`succeeded/0`、`succeeded/0`；模型散列均为 `7bb0dca0335bcf387f46799f6d9e8b6d4ce0345a87ca7708797d0a2f47cb2e64`。
- 三个内层指纹根均内部可重算；各自保存的 Git 差异和状态原始字节与指纹一致，未跟踪文件清单均为空，没有引用 `PROGRESS.md`。
- 每次运行均有 9 个日志；访问日志分别为 1,220、1,184、1,220 条，全部允许，没有最终评估、禁止数据或评分程序访问。
- 专门的 260 字符检查点案例通过：调用者使用普通路径即可验证和挂载，重复挂载和重复发布均被拒绝且目标字节不变。
- 输入仅含 8 个冻结开发流域、Maurer 气象驱动和 1999-10-01 至 2008-09-30 开发区间。
- 快照审核前后均为 252 个物理文件，整树摘要保持 `134bf4668da27713253f3653b3f0cbb1effa2b1c4038178152adc03e4f82e177`。

## 保留证据

独立审核根：`C:\Users\yiqun\AppData\Local\Temp\m3r10-recovery-audit-55c29f26304546c2a54f617e80694501`

- `fresh-recovery-e2e-repro-fail.xml`
- `checkpoint-260-probe`
- `audit_outer.py`
- `audit_recovery.py`

边界：未读取、寻找或评分最终评估期数据；未运行公平基准评分程序；未执行 64 或 531 流域正式搜索；未作超过基线声明。
