# 里程碑零定向复验：第一轮

- 独立任务：`019f6f3d-f352-72c0-b5b3-7c55cf661d35`
- 判定：已确认

## 复验

在两个独立系统临时目录中复制静态属性表，仅改变一个列名，保持总列数为 28：

| 改动 | 选择器行为 | 返回流域数 | 退出状态 |
|---|---|---:|---:|
| `p_mean` 改为 `q_mean` | 正常返回 | 8 | 0 |
| `p_mean` 改为 `model_nse` | 正常返回 | 8 | 0 |

两次均返回原冻结八流域，且快照源文件内容摘要前后一致。复验没有读取观测流量或最终评估文件。

## 处理决定

- 问题涉及数据泄漏边界，阻断下一里程碑。
- 已添加先失败的回归测试，并将选择器改为精确核对 27 个允许静态属性名。
- 修复后相关测试为 15 项通过、0 项失败、1 条既有警告。

