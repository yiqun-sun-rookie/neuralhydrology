# 第三里程碑第六轮四类参考候选原始证据复验

复验结论：`CONFIRMED_FAIL`

第二个完全独立上下文已从原始字节确认第一层失败结论，未把第一层报告内容作为证据。

## 长路径候选物化

- 概念降雨径流源码绝对路径长 268 个字符。
- 普通 `Path.exists()` 为 `False`，`read_bytes()` 和 `shutil.copy2()` 均失败。
- Windows 扩展长度路径成功读取同一 2907 字节，SHA-256 为
  `dda1c977d7623e07052aebaf5927fd3d23cbe51690240b5b98d7c8b00ba59cd7`。
- 定向运行候选物化及登记训练和预测测试，Pytest 退出状态 1、2 项失败，均在 `catalog.py:74` 的普通
  `shutil.copy2()` 处失败。因此这是普通路径兼容问题，不是物理缺失。

## 依赖初始化读取边界

三组合成依赖子进程退出状态依次为 `2/0/2`：源码根未允许时拒绝源码；允许后根内文本成功读取并记录为 `allow`；
旁边良性文本被记录为 `deny`。当前 `stdlib` 和 `platstdlib` 均为 `C:\Users\yiqun\anaconda3\Lib`，覆盖整个
`site-packages`，所以现有运行策略无法证明访问仅限已声明依赖的源码。

## 冻结证据

- 八部分散列 8/8 一致，实际引用 232/232 一致，根散列为
  `184dd693501710c739cd80b2d4e4028efa21a395ae2d3cd0ce0a5c61c89d9d02`。
- 快照清单 134/134、运行包清单 22/22，均为零差异。
- SQLite 完整性为 `ok`，散列链失败 0，导出一致，记录及凭据均为 8/8。
- 冻结结构化测试报告为 151 项、0 项失败、0 项错误、1 项跳过。
- 冻结 Git 差异为 0 字节；Git 状态为 7160 字节、108 个非空空字符分隔字段。
- 复验前后均为 135 个文件、762431 字节，全树散列均为
  `341532cf83df290f57c91bf2ef24c90be3db1d8f76ef224ae12be0cbf9b1ebb3`。

探针和日志保留在
`C:\Users\yiqun\AppData\Local\Temp\m3_round6_raw_verify_c3cb003d42584c06ab1d28fd658b423f`。

边界：未读取、寻找或评分最终评估数据，未运行公平基准评分程序，未执行正式流域搜索，未作超过基线声明；
仓库和冻结快照均未修改。
