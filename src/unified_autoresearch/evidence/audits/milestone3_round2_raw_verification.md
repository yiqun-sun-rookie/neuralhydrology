# 里程碑三第二轮数据边界原始证据复验

`RAW_VERDICT: CONFIRMED_PASS`

## 结论

冻结快照的数据准备边界、证据链和测试后字节不变性均通过独立复验。该结论只覆盖里程碑三第二轮数据准备。

## 合成数据边界

- 有效输入为系统临时目录中的八个流域、六个日期、48个“流域—日期”键；全部数值有限。
- forward和reverse各得到32个训练键与16个验证键；交集为0，并集为48；训练特征与目标、预测特征与验证真值的键分别相等。
- 预测目录只含 `predict_features.parquet` 和 `static_attributes.json`，不含 `qobs` 或 `validation_truth`。
- 同键空日期和非午夜时间戳均触发 `ValueError: source dates must be present daily timestamps`，且最终输出根不存在。
- 清单日期越界、表格日期越界、静态属性漂移、时间序列或静态属性非有限值、重复键、额外文件和硬链接等八类反例均被拒绝且未创建输出根。

## 冻结证据链

- 快照清单114项，连同清单自身共115个文件、634458字节；无缺失、新增或散列差异。
- 运行包清单22项全部匹配，散列为 `ff231ee0b5b16f619cf2b1b57076cd5a21560845e983eb99f79e016fdf58654d`；快照清单散列为 `1e9d153642b5f1bcd7b868621ec5de8107351b3d1166f16b3d8e0ca18b6ca9e9`。
- 八部分数字指纹独立重算最终根为 `5820b07e167e6c2647e8d1de20b518d79de0013b5fec0a4ca5afc5a2c216c0cf`。
- Git源提交为 `ffbde6d9091df08363ff5b7ab0492bdd02873c42`，已跟踪差异0字节，状态含88项未跟踪文件并与清单一致。
- SQLite完整性为 `ok`；8条记录、状态链、导出记录、禁止修改触发器和8份外部凭据全部一致。

## 测试与不变性

- 数据边界定向测试12项通过；冻结全套测试120项通过、1项跳过。
- 测试前后115个文件的全字节清单根均为 `ae5466222bf3bbad73617e310c65ba08ceb7590bf25a46e98d7cdcee4ef82005`，未产生字节码或缓存。
- 本复验未读取、寻找或评分封存最终评估数据，未运行公平评分程序，未执行正式流域搜索。
