# 里程碑三第一轮数据边界原始证据复验

`RAW_VERDICT: CONFIRMED_FAIL`

## 结论

冻结实现确实接受特征表和目标表中同一流域、同为空日期的匹配行，并在构建输出时无异常、无警告地丢弃该行。失败仅针对数据边界完整性；冻结快照、数字指纹和登记证据链本身均通过复核。

## 空日期原始复现

全部输入均为系统临时目录中的合成有限数值。八个流域、六个日期原为每表48行，将两表第一行日期同时替换为空后：

- 输入特征表和目标表各48行，各有47个非空日期和1个空日期；非空最小日期与最大日期仍为1999-10-01和2008-09-30。
- 修改后的特征文件散列为 `e819d3a2c8758bc9fcc2715e4cb1d4e7d60ebbdd1d4398e032d07203dc7d372c`，目标文件为 `c0a208f41900509928ac05297e979c49fdbc44028d9b4abbdacf971a6bfe03df`；源清单中的散列均与当前字节一致。
- 构建异常数和警告数均为0，最终输出根及 `PACKAGE_MANIFEST.json` 被创建。
- forward训练特征、训练目标各31行，预测特征、验证真值各16行；reverse相应为32、32、15、15行。
- 八个输出表均无空日期，被替换行的合成数值没有出现在任何输出中，证实静默丢行。

直接原因是日期转换接受空日期，最小值和最大值忽略空日期，两个相同空日期键又通过集合相等检查，随后日期区间筛选把该行排除。

独立定向测试为10项通过。冻结可扩展标记语言测试报告重算为119项：118项通过、1项跳过、0项失败、0项错误。

## 冻结证据复核

- 快照113个物理文件、112项清单；运行包23个物理文件、22项清单。缺失、额外和散列不符均为0。
- 快照清单散列为 `24b52bc3fae259ba5954b065975b66e0458f3b877b778f4b64e13fe28ac6015c`；运行包清单散列为 `5f2aacdf284d436fddc396f8cb8132e596cdb698e04268b0c3f351d436816189`。
- 八部分数字指纹全部逐字节匹配，最终根重算为 `7c053981660b84a6f7eabb4ff3d7bdd096fc365530dcba672e65138bd3886d79`。
- Git源提交为 `ffbde6d9091df08363ff5b7ab0492bdd02873c42`；差异为0字节；原始状态为5573字节、86项未跟踪文件，全部与清单匹配。
- SQLite完整性为 `ok`；8条记录、5个顺序状态、两个禁止修改触发器及8份外部凭据均一致。
- 测试前后快照文件数均为113，整树散列均为 `62449320208b25b4c802404ad1de22ae8a4de107fe73a0759800ce4adbbbe4c1`。
- 本复验未读取、寻找或评分封存最终评估数据，未运行公平评分程序，未执行正式流域搜索。
