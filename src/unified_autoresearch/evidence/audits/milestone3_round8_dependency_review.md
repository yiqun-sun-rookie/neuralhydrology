# 第三里程碑第八轮依赖源码范围独立审核

审核结论：`PASS`

- `C:\Users\yiqun\anaconda3\Lib` 虽包含 `site-packages`，但 `purelib` 和 `platlib` 被列为受限根；读取受限根时
  必须同时属于启动前记录的声明依赖初始化闭包，标准库父目录不能隐式放行。
- 合成探针中，声明依赖目录内文本读取为 `allow`；同一 `site-packages` 下未声明相邻文本读取为 `deny`、
  返回状态 2、候选入口未执行。
- NumPy 1.26.4、pandas 2.3.3、PyArrow 22.0.0 均精确匹配；初始化闭包共 12 个根，不含整个
  `site-packages`；461 次真实受限根读取全部位于闭包内，错误和拒绝均为 0。
- 初始化完成后，未声明包导入、子进程、网络和本地动态库调用全部被拒绝并记录。
- 四类候选独立参数化复验 4/4 通过；四种方法标识、训练和预测共 8 个运行、Parquet 三列和 4 行有限预测、
  每个运行 8 条登记链、8 份凭据及最终指纹均成立。
- 超过 260 字符的源码复制独立复验 1/1 通过。
- 冻结结构化测试结果为 152 项、0 项失败、0 项错误、1 项跳过。
- 运行包清单 22 项、快照清单 138 项，逐文件原始字节散列和范围一致。
- Git 提交为 `ffbde6d9091df08363ff5b7ab0492bdd02873c42`；差异 0 字节；状态 7506 字节；冻结字节与当前
  实际命令输出相同。
- SQLite 完整性为 `ok`，外键错误 0，8 条记录散列链、导出和 8 份凭据均一致，终态为 `succeeded`。
- 八部分最终根散列重算为 `d5e6f610ba3bf6d930aeee295ad669d108336ce9c8defb16d2d0f40b9ea8ecba`。
- 审核前后快照均为 139 个文件，整树摘要均为
  `0b5b4056ccf08c109cff4552da69154ada92fe2cbaf560bbef0b30cd7926171b`。

独立探针保留在
`C:\Users\yiqun\AppData\Local\Temp\m3r8-independent-audit-b0572762e3394c7ea795b69998fbf37e`。

未读取或寻找最终评估期数据，未运行公平基准评分程序，未启动正式流域搜索，也未作超过基线声明。
