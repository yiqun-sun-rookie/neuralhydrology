# 里程碑一第五修复版定向独立复验

- 独立任务：`/root/milestone1_repair5_targeted_verification`
- 受保护快照：`runs/unified_autoresearch_audits/m1_repair5_1f751b1f922db360_clean`
- 判定：**PASS**。

## 原始证据

- 普通路径与 `\\?\` 扩展路径的 .NET 枚举一致：快照 72 个文件、清单 71 条；运行包 23 个文件、
  清单 22 条。两级集合和散列一致，72 个文件可读，72 个文件和 19 个目录有只读位。
- 8 份 272 字符路径凭据同时进入两级清单。
- 根目录拒绝规则显式、深层规则继承；掩码 278 仅包含写数据、追加数据、写扩展属性和写属性。
  测试前后四次排他创建均为 `PermissionError`，无残留，集合与内容摘要不变。
- 8 个指纹组成和根散列重算一致；指纹、运行元数据、快照元数据和第五修复版名称身份一致。
- SQLite 独立只读核验 8 条，散列链 0 项错误；8 份凭据 0 项错误；两次锁证明一致；终态为 `succeeded`。
- 95 字符系统临时目录、禁用字节码和测试缓存下，JUnit 为 36 项、0 失败、0 错误、0 跳过。
- 访问控制清理只作用于由 `tmp_path` 构建的临时测试快照；冻结快照测试前后访问控制描述符完全一致。
- 未发现当前快照可复现的新阻断；4 条测试警告不影响退出码和 JUnit 结果。
