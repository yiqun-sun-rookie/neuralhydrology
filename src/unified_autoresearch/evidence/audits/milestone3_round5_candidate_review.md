# 第三里程碑第五轮四类参考候选独立审核

结论：未发现影响四类轻量参考候选功能准入的缺口，审核通过。

- 四个候选为独立实现：两隐藏层宽度为 8 和 4 的多层感知器；按流域校准衰减率、比例和偏移的线性水库；静态属性感知回归与水库各占一半的融合；分流域训练均值。四份实现文件散列和预测结果均不同，不是类别别名。
- 合成两流域数据完成四类候选各一次训练和一次预测，共 8 次 `run_registered_candidate` 运行。每次均为成功、拒绝事件 0、登记记录 8 条、凭据 8 份，最终指纹覆盖输出及 7 类日志。四份预测表均为 `basin/date/qsim` 三列、4 行、全部有限值。
- 每次预测仅挂载 `predict_features.parquet`、`static_attributes.json`、`model_artifacts`；未挂载验证目标或其他真值文件。
- 当前有 5 份 NumPy 发行元数据、4 个不同版本；活动解释器实际解析 `C:\Users\yiqun\anaconda3\Lib\site-packages\numpy\__init__.py`，版本 1.26.4。声明非活动版本 2.4.1 时在启动前拒绝，未创建运行策略、访问日志、子进程输出或候选输出。
- 依赖预初始化后的受限运行回归测试 40 项全部通过。冻结完整测试为 149 项通过、1 项因当前 Windows 账户不能创建符号链接而跳过、0 项失败。
- 八部分指纹全部重算一致，根散列为 `4a1f980b61b94d75696995f098d7ab3d95fa51668288a0557c47ae87f49065a9`；228 个被引用文件均与指纹一致。
- 快照清单 132 项、运行包清单 22 项均无缺失、多余或散列错误。Git 提交为 `ffbde6d9091df08363ff5b7ab0492bdd02873c42`；原始状态和当前状态逐字节相同，跟踪文件差异为 0 字节。
- SQLite 完整性检查为 `ok`；8 条记录散列链、状态链、导出记录、不可更新和删除触发器及 8 份凭据全部一致。
- 复验前后快照均为 133 个文件、743269 字节，整树散列均为 `f623d83e02878876f390b1923e959a421bbac9f025f699a78cefb72a1ebb8670`。
- 所有功能复验产物位于系统临时目录。冻结快照长路径必须使用 Windows 扩展长度路径前缀；使用该前缀后全部核验通过，未修改仓库或快照。

边界：未读取最终评估期数据，未运行公平基准评分程序，未执行 64 或 531 个流域正式搜索，未作超过基线声明。

审核结论：`PASS`
