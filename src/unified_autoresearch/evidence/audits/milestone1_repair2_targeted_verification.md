# 里程碑一第二修复版定向独立复验

- 独立任务：`/root/milestone1_repair2_targeted_verification`
- 只读快照：`runs/unified_autoresearch_audits/m1_repair2_4cb937ffe680f249_clean`
- 判定：**FAIL**。

## 通过项

- 长路径安全枚举 64 个物理文件；快照清单 63 项且差集、散列错误均为 0。
- 当前运行包物理文件除清单自身为 22 项，与 22 项清单严格相等；8 份 272 字符路径凭据均进入两份清单。
- 8 个指纹组成、根散列、快照身份、SQLite 8 条记录、8 份凭据、两份锁证明和终态均一致。
- 系统临时目录中的六项精确回归全部通过；保存的 JUnit 为 35 项、0 失败、0 错误。

## 新阻断

`evidence_package.py` 已对快照复制、快照清单和只读设置使用长路径安全枚举，但运行包清单收集仍使用
`final_root.rglob("*")` 后接 `is_file()`。把运行包放在当前深快照路径后，普通枚举只识别 15/23 个文件，
遗漏全部 8 份 272 字符凭据。现有 22 项清单是在较短原路径生成，不能证明深路径生成器正确。

现有回归只断言深快照清单闭包，没有断言深运行根的 `PACKAGE_MANIFEST.sha256` 闭包，因此双通过门槛未满足。
