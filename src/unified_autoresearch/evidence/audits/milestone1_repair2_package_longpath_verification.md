# 里程碑一第二修复版运行包长路径定向确认

- 独立任务：`/root/milestone1_repair2_package_longpath_verify`
- 判定：**CONFIRMED**。

## 原始证据

- 对深快照中的第二修复运行包，长路径安全枚举得到 23 个物理文件，排除运行包清单自身后为 22 项；
  普通 `Path.rglob("*") + is_file()` 只识别 15 项，排除清单自身后为 14 项。
- 差集恰为 `registry/receipts/` 下的 8 份凭据，普通绝对路径均为 272 字符。
- 已保存的 22 项清单与长路径安全物理集合和散列完全一致，因为它是在最长文件路径为 205 字符的原工作区生成。
- 生成代码仍为 `package_files = [path for path in final_root.rglob("*") if path.is_file()]`；
  现有测试只约束深快照清单，没有约束深运行根的运行包清单。

## 下一轮最小检验

在足够深的运行根创建 8 份凭据，先断言其绝对路径超过 260 字符，再要求：

`运行包清单路径集合 = 长路径安全枚举的运行包物理文件集合 - 运行包清单自身`。
