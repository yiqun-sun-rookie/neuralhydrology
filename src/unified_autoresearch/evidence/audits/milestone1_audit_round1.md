# 里程碑一第一轮独立审核

- 独立任务：`019f6f55-6b06-78c2-b5de-1ed9e080861b`
- 只读快照：`runs/unified_autoresearch_audits/m1_70589de0d002f84b_clean`
- 冻结根散列：`70589de0d002f84b07d44603ce20ee43fac47b54fb85a05301c33788895c5ea2`
- 最终评估数据：未读取、未寻找、未评分。

## 正常路径证据

- 27 项测试通过，用时 10.22 秒；0 项失败。
- 数字指纹内部校验通过；67 条清单记录、33 个不同文件，逐文件重算 0 处不符。
- 冻结的版本控制差异和状态字节散列均相符；根散列与指定值完全一致。
- 登记副本共 8 条记录，完整性失败 0 项，最终状态为 `succeeded`。
- 清除临时副本只读属性后，更新和删除均被数据库不可变触发器拒绝。

## 阻断完成的问题

独立任务以实际最小复现确认下列 5 项问题；任务在补充行号前发生系统错误，因此不把它视为通过审核：

1. 不持有调度锁的调用方仍能把状态从 `queued` 转为 `running`。
2. 失效锁检查与文件替换之间存在竞争窗口，可归档绑定当前存活进程的新锁。
3. 移除删除触发器并删除散列链最后一条记录后，`Registry.verify()` 仍报告 0 项失败。
4. 被版本控制忽略、且没有显式列入 `artifact_paths` 的产物不改变根数字指纹。
5. 直接插入“外层记录散列正确、内部指纹详情与组成散列不一致”的更正记录后，`Registry.verify()` 仍报告 0 项失败。

结论：**FAIL**。五项均需第二个独立任务定向重跑，确认后才修复。
