# 交接：比例Q修复 03C 完成 + 顽固流域同病确诊（2026-08-14）

**当前结论：土壤最大蓄水容量候选辨认线的根因已破案并修复到位——滤波器过程噪声与真值
SLZ 对数正态噪声错配（方差低估约320倍）导致创新误塞进土壤水 SM、方差坍缩、概率锁死。
状态比例 SLZ 过程噪声（矩匹配，multiplier=1）在90流域03C重跑中使86–90%任务概率指标
改善、90天窗六类转移通过率首次全部≥0.5；30天窗增容方向是物理性慢检（中位响应13–37天）
不可达。残余4–5个顽固流域经诊断确认为**同一种病的未覆盖通道**：SM 自身过程噪声为零，
其误差仍会积累（六任务全部 SM 主导 z=+32~+108，且 03049800·s1 上只替换 SM 一个状态即
解除末段灾难 0.017→0.70）。下一步唯一候选修复方向是给 SM 增加过程噪声，未获授权勿自启。**

## 一、本次会话完成的四段工作

### 1. 第540天子集反事实（07184000·s0）：SM 充分且必要

| 替换内容 | 第540天事件 | 540–719平均真候选概率 | 零概率日 |
|---|---|---:|---:|
| 全15状态（既有冻结实验） | 通过 | 0.6613 | 0 |
| 只换 SM（索引2） | 通过 | 0.6655 | 0 |
| 只换 SLZ / 只换路由 / 除SM外全换 | 均不通过 | ~1e-26 | 156 |

误差形成：SM 误差在第445–471天新生成（非累积），同期标准差 34→6 坍缩；估计停在
400–425 而真值约200–250（双倍容量候选 parFC=487，非贴顶）。

### 2. 根因（代码事实）

- 真值每日注入 SLZ 对数正态噪声 σ=0.02（SLZ≈3500 时日标准差≈70 mm）。
- 冻结03B合同滤波 Q=fixed/3e-8/仅SLZ项 → SLZ 项标准差≈3.9 mm（方差差~320倍），
  **其余14状态过程噪声为零**。
- Q 是三候选共用背景设置（2026-08-07 公平性修正），不是 IMM 候选维度。
- 链条：噪声无处归因→创新塞进SM→概率锁定后混合方差注入消失→SM方差坍缩→
  减容切换时守恒映射把幻影土壤水倒进SUZ→真候选似然被摧毁→浮点下溢锁死。

### 3. 单流域修复 + 90流域03C重跑（360任务全成功，独立汇总核验 passed）

修复=q_mode `lower_groundwater_state_proportional`（multiplier=1，每日按候选0转移预测
的参考 SLZ 重建 Q[4,4]，三候选共用；代码库原有开关）。单流域（07184000×2种子×2方法）：
零概率日全归零、NLL≈0.40、SM 误差 58σ→1.3σ、两方法行为收敛一致。

90流域（判据双报按预登记执行，跑前登记）：

| 窗宽 | 方法 | 总通过 | 减容 | 增容 | 每类≥0.5门 |
|---|---|---:|---:|---:|---|
| 30天主档 | C | 700/1080 | 0.926 | 0.370 | ✗ |
| 30天主档 | P | 708/1080 | 0.935 | 0.376 | ✗ |
| 90天副档 | C | 896/1080 (0.830) | 0.937 | 0.722 | **✓ 全过（0.68–0.99）** |
| 90天副档 | P | 903/1080 (0.836) | 0.943 | 0.730 | **✓ 全过（0.69–1.00）** |

- 减容中位响应0天；增容中位13–37天 → 30天窗对增容是物理下限不可达。
- 零概率日：C 2044→1215、P 814→256；配对 Brier 改善 157/155 任务、NLL 改善 162/154 任务
  （每方法180任务）。30天事件数持平略降（C 700 vs 706、P 708 vs 717）——03B 基线含
  "锁死撞对"0天假通过。
- 概率质量门聚合仍 FAIL 但尾部集中：C 10/180任务（5流域）、P 4/180任务（4流域）有零概率日；
  中位任务 NLL≈0.41–0.42；161/180(C)、165/180(P) 任务单独过 NLL≤log3。
- 两方法在90流域上仍几乎不可分（<1%）：此前 C/P 之争大半是噪声错配伪影。

### 4. 顽固流域诊断：同一种病，SM 通道未覆盖

只读分解（P方法六个受灾任务，真值重放全部逐位通过）：崩塌前夕主导候选（p≈0.99–1.0）
的 SM 误差全部为正、z=+32~+108，第二名状态 z≤11；方差坍缩到毫米级（02430085·s1 的
SM 标准差仅 0.7 mm）。四个任务崩在阶段5（真值=半容量、锁在中心容量），07145700 两种子
崩在阶段3（真值=中心、锁在双倍）。

因果确认（03049800·s1，最重案例）：第1000天只替换主导候选 c0 的 SM 均值
（182.45→真值121.46，其余全部不动，前1000天与保存运行逐位一致）：
阶段5零概率日 30→0、平均真候选概率 0.294→0.521；**末段(1080–1259) 0.017→0.701**
（基线整段锁死，反事实恢复）；30天窗事件数不变。

结论：比例 SLZ Q 修复对这些流域不完备——SM 过程噪声为零的通道仍在积累误差。
候选修复方向（未测试、未授权）：给 SM 加过程噪声（如小幅状态比例或降雨比例项）。

## 二、关键产出与路径（全部在 G:\wt\camels-rising）

- **提交**：`8c245916`（分支 codex/camels-rising-half-recal，**未推送**）——11个文件：
  4个新模块+1配置+1测试+1预登记文档+4个此前会话遗留的已跟踪修改(g2/imm/preflight/测试)。
- 03C 配置：`src/camels_switch_confirmation/configs/camels_parfc_transition_path_03c_design90_propq.json`
  sha256 `10ef3c1b75e23abba855e35cf922fd7a67c01edb5412e8e4d2d5a7d1efcc1438`。
- 预登记：`docs/plans/2026-08-14-camels-parfc-transition-path-03c-design90-propq-prereg-v01.md`。
- 03C 结果根（360 npz + runner_summary + verification/，未入git）：
  `results/23_camels_switch_confirmation/camels_parfc_transition_path_03c_design90_propq_s01_20260814_local`
  ；`verification/verification_summary.json` 状态 passed，含逐任务 `task_metrics.csv`。
- 子集反事实结果根：`…/camels_parfc_day540_state_subset_counterfactual_07184000_s0_01_20260814_local/`
  （sm_only/slz_only/sm_slz/routing_only/all_but_sm 五变体）。
- 单流域修复结果根：`…/camels_parfc_slz_state_proportional_q_07184000_01_20260814_local/{C,P}_s{0,1}`。
- 顽固流域反事实：`…/camels_parfc_sm_counterfactual_03049800_s1_01_20260814_local/`。
- 新模块：`run_day540_state_subset_counterfactual.py`、`run_slz_proportional_q_single_task.py`、
  `run_parameter_path_design90_propq.py`、`summarize_parameter_path_design90_propq.py`；
  测试 `test/test_camels_parameter_path_design90_propq.py`（7项过；全套既有127项亦过）。
- 概率对比图：会话scratchpad（临时），论文用需重生成，脚本逻辑见本次会话记录。

## 三、已知瑕疵与诚实边界

1. `run_parameter_path_design90_propq.py` 在360任务跑完**后**加了汇总模式参数，与冻结配置
   哈希漂移；已作为唯一声明例外记录在 `verification_summary.json` 的
   `post_execution_implementation_drift`，且核验确认运行本身在冻结实现下执行。提交
   `8c245916` 中的文件是修订后版本。
2. multiplier=1 使用合成噪声已知 σ；真实观测需在线噪声估计（未做）。
3. 顽固流域读只诊断覆盖 P 方法6任务；C 方法任务的 npz 无状态审计数组，如需 C 端诊断须带
   审计重跑。02096846（仅C端受灾）未诊断。
4. 30天/90天判据取舍是论文决策，未定；90天副档系跑前预登记，引用合法。
5. 完整状态精度、预报价值、噪声候选辨认、真实观测同化价值：全部未测，不得声称。

## 四、下一步候选（均需用户明确授权）

1. **SM 过程噪声修复实验**（首选）：单流域（03049800·s1 + 07145700·s0）加小幅 SM 过程噪声
   （建议先扫 状态比例 1e-6~1e-3 或固定 (1~5mm)² 两族），判据沿用03C门；成立再上90流域03D。
2. C 方法端顽固流域带审计重跑诊断（02096846）。
3. 论文对表：本线"噪声诚实后 IMM 可在90天尺度辨认参数候选+方向不对称是物理"与主线
   "IMM 做参数是错误工具（Regge真实案例）"的叙事整合；30/90天判据决策。
4. multiplier 敏感性、在线噪声估计立项。

## 四b、当日追加（用户"all"授权后）：C端确诊、SM加噪扫描、03D降雨比例噪声90流域重跑

1. **推送完成**：`8c245916` 已推 origin；当日新增工作见后续提交。
2. **C端02096846确诊同病**（重放逐位验证）：崩塌前主导候选 SM 误差 +65~+69 mm、
   z=+257,000~+587,000（完整交互下 SM 方差坍缩至 0.0001 mm 级）。8/8 顽固任务同一病灶。
3. **SM加噪两轮扫描**（8受灾+2健康对照×10方案，结果根 `camels_parfc_sm_noise_scan_{01,02_rain}_20260814_local`）：
   固定/SM比例噪声存在硬权衡（治愈越全、健康对照越伤：sm_prop_0p02 治愈8/8但对照NLL×8）；
   **降雨比例 sd=0.2×日降雨 为权衡最优**（受灾零日517→9、对照NLL 0.403→0.414无实质损、事件不变）；
   已知不响应者 02193340·s1（严重日反升）。
4. **03D（预登记后360任务全成功，内置重算汇总）**：
   结果根 `camels_parfc_transition_path_03d_design90_smrain_s01_20260814_local`；预登记
   `2026-08-14-…03d-design90-smrain-prereg-v01.md`（含选择偏差警告）。
   - P方法：零日 256→**17**、严重日 603→215、**平均NLL 0.977 首次过 log3 门**、中位任务 0.409、
     90天窗 899/1080 六类全≥0.5（min 0.706）、30天窗 703 持平。零日残余仅3任务
     （02193340×2=预告的不响应者、03049800·s1 残余2天）。
   - C方法：零日 1215→549、严重日 1472→1228、NLL 41.5（仍未过门）；残余7任务
     （02193340/02221525/07145700 各两种子 + 12377150·s0 轻度新增2天）。07145700 C端未愈
     （扫描只测了它的P端）——C端（完整状态交互）对SM腐坏更脆弱。
   - 健康任务配对Brier/NLL约半升半降（微小变化），与扫描对照预测一致。
5. **全线轨迹（P方法）**：零概率日 814(03B)→256(03C)→17(03D)；平均NLL 653→32.6→0.977。
   诚实边界：03D方案在含全部受灾任务的扫描上选出（预登记已声明选择偏差）；零日/严重日=0
   的严格门仍未达成；C方法尾部问题未收口。

## 五、新任务首查命令

```powershell
Set-Location -LiteralPath 'G:\wt\camels-rising'
git log --oneline -3
git status --short --branch
```

预期：HEAD=`8c245916`，分支 codex/camels-rising-half-recal；未跟踪条目为历史证据+
本次新结果根，属正常。

```powershell
$env:PYTHONPATH='G:\wt\camels-rising\src'
& 'C:\Users\yiqun\anaconda3\python.exe' -m pytest -q -p no:cacheprovider test/test_camels_parameter_path_design90_propq.py
```

预期 `7 passed`。科学汇总权威文件：
`results/23_camels_switch_confirmation/camels_parfc_transition_path_03c_design90_propq_s01_20260814_local/verification/verification_summary.json`。

## 六、回答规则（沿用）

中文、结论先行、事实/推断/未验证分开、每次回复以"## 结论"结束、删除过程性叙述。
