# 历史连续多尺度气象模型版本09目标完成度审计

## 结论

完整目标尚未完成，当前不能训练、不能正式预测、不能评分，也不能宣称531流域有效或无效。已证明的是协议几何、核心经典模型计算、旧八随机数参考身份和保护边界；未证明的是531流域输入、30轮严格嵌套、三个模型家族八随机数训练、正式预测、一次性评分和独立最终审核。

最关键的边界是：版本09能零容差复现核心 `CudaLSTM` 的模型与单步优化计算，但不能声称复现旧参考
的完整训练数据轨迹。旧参考只是不参与判定的历史参考；同一无泄漏正式训练合同下的
`B09-CLASSIC`清洁经典集合才是主比较基准。

## 逐项证据

| 完整目标要求 | 当前权威证据 | 判定 |
|---|---|---|
| 只使用 Maurer 气象和27项静态属性 | 协议冻结5项 Maurer、27项静态属性和禁止输入；正式输入尚未生成 | 部分证明 |
| 最长历史滞后3,561天并冻结规则 | 121个边界、120个历史块、滞后270至3,561、边界哈希和覆盖测试已冻结 | 已证明 |
| 经典256单元、270天近期路径与核心实现一致 | 合成输入上参数、输入顺序、完整序列丢弃、预测、随机数、损失、梯度、Adam状态和更新参数零差异 | 已证明模型计算，不等于完整训练轨迹 |
| 停用历史后严格复现 | 合成单步锁步零差异；531流域30轮、204,630次更新尚未运行 | 部分证明 |
| 30轮、八个训练随机数 | 协议和四个实验配置已冻结100至800、30轮和学习率计划；没有正式训练 | 部分证明 |
| 连续历史候选和同参数量控制 | 模型参数量分别为596,737和595,198，结构测试通过；没有正式检查点 | 部分证明 |
| 531流域正式输入完整且无泄漏 | 目标包和 Maurer 内存映射存储不存在，授权为 `false` | 缺失 |
| 全部预测、代码、输入和环境指纹封存 | 旧参考8个运行、16个文件已核验；版本09训练产物不存在 | 部分证明 |
| 只调用一次正式评分服务 | 清洁配对路线已固定一次后封存随机划分、一次调用和零重试；代码未实现，评分授权为 `false` | 缺失 |
| 完整覆盖及五个统计成功门槛 | 没有正式预测或评分结果 | 缺失 |
| 独立审核代码、结构、边界、产物和结论 | 当前只有实施者自审；最终独立审核尚未发生 | 缺失 |
| 本机内存安全 | 31.70 GiB本机的启动、保留、进程和单次分配门已实现；本轮长任务检查低于12.68 GiB | 已证明保护有效，长任务为 NO-GO |

## 当前可复现状态

- 分支：`codex/historical-band-experts-pilot`
- 核心修复提交：`86f20292c040b50ea7c7cfd7ae94ba273cd413d5`
- 协议 SHA-256：`b81bce8fc83aa8c4cad2d36475c6e6da553567f54b5f5f8d52457006fb446ed8`
- 专项测试：19项通过，1个仓库既有 pytest 配置警告
- 完整局部测试：2026-07-31在提交
  `23575402ab2bae8857ecf80c3081d16af64434a0`上首次通过，
  `378 passed, 1 warning in 48.35s`；完整证据见
  `docs/technical/historical_multiscale_formal_v09_full_test_audit.md`
- 旧参考验证：8个运行、16个文件、随机数100至800和提交前缀全部通过
- 冻结目录和正式评分代码改动：无
- 正式版本09结果目录：不存在

## 下一有效动作

1. 完整局部测试已经在当前代码提交上通过；除非出现后续代码提交，否则不得重复运行；
2. 先取得清洁同信息配对评分路线的精确实施批准，只实现批准文本逐项列明的模块和合成测试；
3. 清洁路线实现和完整测试通过后，先取得正式输入代码实现批准，完成代码、合成测试和独立审核；
4. 输入代码冻结后再单独取得一次真实输入生成授权，生成并审核流式输入；
5. 输入产物独立重载通过后，依次申请训练代码实现、531流域严格嵌套和主训练授权；
6. 任何严格嵌套差异、哈希漂移、禁止输入命中或内存门失败都立即停止；
7. 训练和状态诊断独立审核通过后，分别申请预测代码实现和一次真实预测授权；
8. 正式评分和不重评分的最终独立审核保持为最后两个独立阶段。

当前路线实施批准文本固定为：

```text
批准版本09清洁同信息配对评分路线及三组预测封存后由唯一评分进程抽取256位随机数派生107流域留出规则；仅授权编写清洁评分合同、三集合评分包构建器、新评分入口、一次性授权与随机划分模块、评分前置审核器、最终不重评分审核器及合成测试，不批准生成正式输入、训练、正式预测或评分。
```
