# 历史连续多尺度气象模型版本09跨阶段可复现性审核

## 结论

当前状态为`NO-GO`，完整正式实验属于“部分可复现”，不能表述为已经可运行或已经有531流域结果。

已经冻结并可核验的是科学协议、现有版本09实现、合成测试证据和四阶段实施计划。尚不存在正式输入、
严格嵌套运行、24次主训练、正式预测或正式评分产物；这些结果目前均无法确定。

本次审核补齐了后继阶段对全部前置审核报告的SHA-256绑定，新增旧八随机数检查点的函数桥接门，
并修正唯一评分报告的单写入者设计。没有生成正式数据，没有训练、预测或评分，也没有读取正式评估观测。

## 当前只读状态

检查日期：2026-07-31。

- 分支：`codex/historical-band-experts-pilot`
- 检查起始提交：`6b4338a9c11787e7795a404584dfbc60a1b08c64`
- 检查起始工作区：干净
- 协议SHA-256：
  `b81bce8fc83aa8c4cad2d36475c6e6da553567f54b5f5f8d52457006fb446ed8`
- 正式输入生成、训练、正式预测、正式评分授权：全部为`false`
- 正式结果目录：不存在
- 受保护冻结目录和评分代码相对版本09实施父提交的差异：0
- 当前代码相对完整局部测试提交
  `23575402ab2bae8857ecf80c3081d16af64434a0`的差异：0
- 既有完整局部测试证据：
  `378 passed, 1 warning in 48.35s`

本次只有文档变化，因此没有把既有378项测试重复运行并冒充新代码证据。以后任何Python实现变化都必须
在对应提交重新运行完整局部测试。

## 科学协议对齐

当前协议的几何、信息和评分部分与正式目标一致：

- 531个冻结流域；
- 动态输入仅5项Maurer气象，静态输入仅27项属性；
- 正式评估期1989-10-01至1999-09-30，每流域3,652天；
- 最早正式目标日使用滞后0至3,561天，共3,562个因果日；
- 近期路径固定为滞后0至269天；
- 历史路径固定为滞后270至3,561天的120个连续对数分箱；
- 经典近期长短期记忆网络隐藏宽度256；
- 连续历史候选的历史与近期长短期记忆网络隐藏宽度均为256；
- 同参数量近期控制隐藏宽度369；
- 训练30轮，随机数100、200、300、400、500、600、700、800；
- 唯一评分对象是连续历史候选第30轮八随机数算术平均；
- 成功门为公开流域配对中位提升至少0.01、Wilcoxon检验p值小于0.05、
  10,000次自助抽样区间下界大于0、秘密流域提升至少0.005且保留公开提升至少50%。

受保护评分代码当前实现的门槛、10,000次自助抽样和随机数0与上述合同一致。

但“逐元素复现冻结经典基准”必须拆成两个可验证命题：

1. 冻结旧检查点的函数映射是否能由版本09干净经典路径和停用历史路径逐元素复现；
2. 新正式管线中，干净经典路径和停用历史路径能否在531流域、30轮的每次更新中保持逐元素相同。

当前协议明确不声称重建旧模型的完整训练轨迹。八个旧运行当前可核验的训练证据包括第10、20、30轮
模型与优化器、训练归一化文件和日志中的逐轮平均损失；顶层和`train_data`目录没有批次顺序、
逐步随机数状态、逐步参数或训练源文件哈希。旧正式测试文件没有读取。现有证据不能证明旧训练轨迹
逐步重现；不能把新管线锁步相等改写成旧轨迹已重现。

### 旧八随机数检查点只读函数桥接

本次从协议登记的旧结果根目录只读取八份配置和八个第30轮检查点，没有读取旧`test_results.p`、
训练或正式评估流量。对随机数100至800逐项完成：

- 检查点SHA-256与协议登记值全部一致；
- 同一检查点严格加载到核心`CudaLSTM`和版本09干净经典模型；
- 停用历史模型只缺少6个不会执行且不可训练的历史参数键，活动参数全部加载；
- 固定合成输入为2个样本、270天、5项动态和27项静态属性，生成随机数29,090；
- 在中央处理器和NVIDIA GeForce RTX 4070 Ti上，核心、干净经典和停用历史三条路径的八组输出
  全部逐元素相同，每组最大绝对差均为0；
- 本次环境为Python 3.11.5、PyTorch 2.2.2、CUDA 12.1、cuDNN 8.8.1和驱动595.95。

这是实际冻结检查点到版本09路径的函数兼容证据，不是旧训练轨迹或531流域正式预测复现证据。
本次命令尚未由已提交的专用审核器封存；正式训练前还必须由独立审核器在中央处理器和正式训练设备上
重复该门，并封存报告。

## 跨阶段证据链

| 阶段 | 必须产生的核心证据 | 下一阶段必须绑定的证据 |
|---|---|---|
| 正式输入 | 输入授权收据与消费收据、`input_attempt_01/seal.json`、输入产物外部审核、可信训练目标来源外部审核 | 输入封存和两个目录外审核报告的SHA-256 |
| 严格嵌套 | 旧参考函数桥接审核、严格嵌套授权与消费收据、种子100运行封存、严格嵌套外部审核 | 输入三项证据、旧参考桥接、严格嵌套运行封存和外部审核SHA-256 |
| 24次主训练 | 主训练授权与消费收据、24个运行封存、训练外部审核、`training_seal.json` | 输入、旧参考桥接、严格嵌套、训练外部审核、24个第30轮检查点和训练总封存SHA-256 |
| 正式预测 | 提交配置、预测授权与消费收据、24个逐随机数预测、3个固定集合、候选源码包、预测外部重放审核、预测总封存 | 全部前置审核、唯一候选文件、源码包、重放报告和预测总封存SHA-256 |
| 唯一评分 | 评分合同、评分前独立审核、具体候选哈希授权与消费收据、前置报告、尝试日志、服务报告、账本唯一追加 | 终审绑定服务报告、账本前后快照、预测哈希、调用次数和全部前置封存 |
| 最终独立审核 | `final_audit.json`、最终独立审核文档、治理侧车、研究登记和唯一账本追加提交 | 不再产生第二次评分或基于结果的选择 |

每一阶段授权的`prerequisite_sha256`必须对键集合和取值同时做完整相等验证。不能只绑定一个总封存后，
依靠文字声称目录外审核已经通过。

## 本次发现并修正的断点

### 1. 阶段授权接口不能安全扩展

训练计划曾把若干输入哈希写成固定函数参数，预测计划仍使用更短的旧接口。这样实现时容易遗漏新增加的
目录外审核报告。

现统一为`prerequisite_sha256`映射。每个动作冻结允许的精确键集合；未知键、缺失键或任一哈希漂移
都必须拒绝。

### 2. 训练总封存没有明确绑定两个输入外部审核

输入目录内部封存不能替代目录外的输入产物审核和可信训练目标来源审核。训练总封存现明确绑定两份报告、
严格嵌套运行封存和严格嵌套外部审核，避免预测阶段只看到“训练完成”而失去输入来源证据。

### 3. 预测授权和预测总封存没有逐项闭合前置审核

预测授权现必须绑定输入封存、两个输入审核、旧参考函数桥接、严格嵌套封存与审核、训练封存与审核、
24个第30轮检查点和提交配置。预测总封存也必须保留这些审核报告哈希。

### 4. 正式评分报告存在两个写入者

受保护评分命令的`--out`会自行写报告；旧计划又要求外层包装器写同一
`score_report.json`，无法证明哪次写入形成最终文件。

现固定评分服务唯一写入`score_report.json.tmp`。服务正常退出后，外层只验证临时JSON并原子改名，
不得重写内容。受保护服务先追加账本、后写报告；因此评分后异常可能形成“账本已追加但报告不完整”。
这种情况必须保持授权已消费、禁止重试，并由终审判为`HOLD`。

### 5. 评分授权没有绑定评分前独立审核

具体候选哈希的评分授权现必须同时绑定
`pre_score_independent_audit.json`的SHA-256，防止审核后替换候选、源码包、账本快照或评分合同。

## 事实、推断与未知

事实：

- 现有科学协议的日期、模型宽度、训练随机数和成功门与正式目标一致；
- 八个实际冻结检查点在中央处理器和当前图形处理器合成输入上可由核心、干净经典和停用历史路径
  逐元素等价执行；
- 现有旧运行证据不足以证明完整训练轨迹重现；
- 受保护评分函数确实先追加账本，命令入口随后才写`--out`报告；
- 当前正式授权均为`false`，正式结果目录不存在；
- 当前没有531流域正式性能。

推断：

- 按修订后的哈希传递、一次性收据和单写入者流程实施，可以形成可追踪到输入、检查点、预测和账本的
  完整证据链。

未知：

- 531个流域的可信训练目标是否全部完整、有限；
- 严格嵌套在531流域30轮训练中是否保持逐元素零差异；
- 24次训练和27个预测文件是否能在资源门内完成；
- 连续历史候选是否通过任何正式性能门。

## 继续条件

当前仍只能等待用户逐字批准正式输入阶段：

```text
批准版本09正式输入生成阶段；不批准训练、正式预测或评分。
```

在此之前不得实现正式输入代码、生成531流域目标包、训练、生成正式预测或调用评分服务。
