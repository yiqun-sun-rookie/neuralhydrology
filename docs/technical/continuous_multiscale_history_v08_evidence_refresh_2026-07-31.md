# 连续多尺度历史气象模型版本08证据刷新

## 结论

2026-07-31只读复核确认：版本08的60流域内部筛选证据没有发生文件漂移，原独立审核的
`complete_multiseed_go`结论仍可由当前本地保存产物重算。

该结论只支持把连续多尺度历史模型作为531流域正式候选；不支持正式泛化、正式胜出、
历史气象因果贡献或物理水龄解释。

## 当前状态

- 分支：`codex/historical-band-experts-pilot`。
- 复核起点提交：`c30d9a13c59ede2f2abf8596bdf5ba497d98b91a`。
- 复核前工作区：干净。
- 结果根目录：
  `results/26_historical_band_experts/continuous_multiscale_history_v08`。
- 结果目录被 Git 忽略，仍只保存在本机；这是长期可追溯风险。

本次没有训练、没有生成预测、没有读取531流域正式评估观测、没有调用正式评分服务，
也没有运行完整测试套件。

## 产物完整性

使用 Python 标准库直接读取12份清单并逐项计算 SHA-256：

- 清单数：12；
- 声明产物数：62；
- 文件缺失：0；
- 哈希不一致：0。

该结果与2026-07-29独立审核的`62/62`一致。

## 预测覆盖

直接读取15个保存预测文件：

- 九个训练运行；
- 一个严格嵌套运行；
- 两个历史门诊断；
- 三个三训练随机数等权集成。

每个文件均满足：

- 43,800行；
- 60个流域；
- 每个流域730天；
- 日期为`2004-10-01`至`2006-09-30`；
- 重复流域—日期键为0；
- 观测和预测全部有限。

## 严格嵌套

停用历史路径的严格嵌套预测、同一运行中的经典近期预测和单独训练的经典近期预测满足：

- 流域—日期键逐元素相同；
- 观测逐元素相同；
- 两组模拟流量逐元素相同；
- 最大绝对预测差为`0`。

这证明版本08在当前60流域内部训练轨迹中实现了最终预测的严格嵌套。它不能替代版本09在
531流域、正式输入和30轮训练上的重新证明。

## 独立数值重算

本次没有导入版本08分析模块；直接按
`1 − 模拟误差平方和 / 观测离均差平方和`
计算逐流域纳什－萨特克利夫效率系数。

训练随机数100：

- 候选跨流域中位数：`0.684935176001`；
- 经典256单元近期模型：`0.660487138269`；
- 369单元同参数量控制：`0.642667697804`；
- 候选相对经典配对中位提升：`+0.011229757423`；
- 候选相对同参数量控制：`+0.017759115270`；
- 优于经典：`38/60`；
- 100,000次自助抽样95%区间：
  `[+0.002520839289, +0.028114808171]`。

三个训练随机数等权集成：

- 候选跨流域中位数：`0.680546813420`；
- 经典256单元近期模型：`0.667771955226`；
- 369单元同参数量控制：`0.663885033585`；
- 候选相对经典配对中位提升：`+0.015202985676`；
- 候选相对同参数量控制：`+0.003592744041`；
- 优于经典：`42/60`；
- 100,000次自助抽样95%区间：
  `[+0.006701713330, +0.022879860265]`。

自助抽样固定使用随机数`20260728`。重算结果与保存摘要在输出精度内一致。

## 与正式版本09的边界

版本08历史滞后为`270–3649`天，总因果窗口3,650天。正式版本09因最早正式日期的气象覆盖
限制，已经冻结为：

- 近期滞后`0–269`天；
- 历史滞后`270–3561`天；
- 总因果窗口3,562天；
- 120个连续时间块。

因此，版本08证据只负责候选选择，不能直接移作正式版本09结果。版本09仍必须在正式输入上
重新完成停用历史路径的零容差嵌套证明，再训练经典近期控制、同参数量控制和连续历史候选。

## 可复现性等级

- 保存结果链：`auditable but not rerunnable`，当前本地预测、检查点、清单和指标完整，
  但不能保证逐位重现历史训练。
- 重跑准备：`partially reproducible`，历史运行清单缺少训练时环境、全部输入和代码提交的
  内嵌密码学绑定。
- 正式531流域结论：`not yet evaluated`。

正式版本09输入计划已经针对这些缺口要求固定代码、输入、环境、授权、来源摘要和两级封印。
