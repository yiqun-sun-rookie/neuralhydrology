# Training Scripts Analysis (Legacy Archived)

This analysis was moved to:
- `docs/archive/legacy_guides/TRAINING_SCRIPTS_ANALYSIS.md`

Current canonical training command:

```bash
python -m neuralhydrology.nh_run train --config-file <config.yml> --gpu 0
```
