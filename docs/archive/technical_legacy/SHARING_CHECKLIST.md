# Sharing Checklist (Legacy Archived)

This file was moved to:
- `docs/archive/legacy_guides/SHARING_CHECKLIST.md`

Current training entrypoint:

```bash
python -m neuralhydrology.nh_run train --config-file <config.yml> --gpu 0
```
