# 流域 07184000 第 540—720 天概率崩塌诊断计划

**范围：** 仅流域 `07184000`、设计种子 `0`、九路径观测后合并方法；不得修改冻结结果，不得扩大到其他流域或种子。

**已观测失败：** 真实最大蓄水容量从两倍值切回中心值后，真实候选概率在第 564 天发生浮点归零，并持续到第 719 天。

**可证伪假设：** 观测误差方差相对真实过程扰动过小，使第 564 天强降雨事件产生极端高置信度的错误似然；单独放大滤波器观测误差方差应保留真实候选的状态路径并消除该时段的概率归零。

**单因素试验：** 保持合成真值、合成观测、过程误差、参数候选、状态映射、转移概率和九路径算法不变，只把滤波器观测误差方差乘数从 `1` 改为 `64`。结果写入全新的隔离目录。

**判断：** 同时报告第 540—719 天的归零天数、平均真实候选负对数概率、事件是否通过，以及全 1260 天对应指标。单流域改善只支持机制诊断，不支持九十流域有效性。
