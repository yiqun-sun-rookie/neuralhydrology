# CAMELS-US 参数路径观测后压缩 03B 实施计划

**目标：** 在不改变水文实验合同的前提下，移除无效的浮点重组相等门，固定三目标逐级全局后验，并用独立逐位重建和逐级高精度参考核验完成一次 20 任务机制检查。

**边界：** 只写 `G:\wt\camels-rising`；不提交、不推送、不删除旧证据；只用种子 0、1；不运行 90 或 531 个流域。

## 任务 1：冻结最终预注册

文件：

- `docs/plans/2026-08-11-camels-parfc-transition-path-mechanism10-amendment-03b-final.md`

步骤：

1. 两名独立审查者核对权重身份、唯一主结果、逐级高精度公式和运行边界。
2. 修复所有阻断后计算 SHA-256；从此不再调整数值常数。

## 任务 2：生产路径算法，测试先行

文件：

- 修改 `src/hbv_joint_uncertainty/imm.py`
- 修改 `test/test_hbv_joint_uncertainty_imm.py`

步骤：

1. 先加入失败测试：普通边际概率唯一递归、平移均值、零质量、次正规数、非对称协方差、03/03A 回归。
2. 让路径模型显式产生并返回 `w、mu_raw、beta、alpha_raw、a`、稳定对数概率和活动掩码。
3. 目标内层与外层使用带权重总量残差校正的平移不变混合；唯一主结果为外层三目标合成。
4. 删除直接九路径与逐级结果的停止断言；保留完整直接诊断。
5. 确认旧交互多模型路径逐值兼容。

## 任务 3：任务输出与审计数组，测试先行

文件：

- 修改 `src/camels_switch_confirmation/g2_switch_confirmation.py`
- 修改 `test/test_camels_switch_confirmation.py`

步骤：

1. 先加入缺少权重、轴、目标矩、主全局矩和跨日来源数组时失败的测试。
2. 仅为路径观测后压缩方法保存最终预注册要求的原始和派生数组及轴元数据。
3. 旧方法不得新增大型路径数组或改变旧数值输出。
4. 确认单任务原子写出和既有目录拒绝覆盖。

## 任务 4：独立核验器，测试先行

文件：

- 修改 `src/camels_switch_confirmation/verify_parameter_path_mechanism.py`
- 修改 `test/test_camels_parameter_path_mechanism_verifier.py`

步骤：

1. 先加入生产值篡改、轴交换、递归断链、环境变化和高精度越界的失败测试。
2. 从来源普通概率、转移矩阵和路径似然独立重建普通路径权重、稳定对数概率、目标矩和唯一全局矩，不调用生产私有函数。
3. 实现正负零规范后的逐位比较。
4. 实现至少 100 位、成对向外舍入的 Decimal 区间、浮点最小间隔包络和逐级均值/协方差门。
5. 独立核对第 0 日普通概率和候选初值；根据候选参数表与每日真实参数重新推导真实候选索引，并核对固定评分切片。
6. 输出每任务及总体最大高精度误差比，以及非阻断的直接九路径诊断。
7. 只在全部完整性通过后计算两项科学继续指标。

## 任务 5：机制运行器和注册安全

文件：

- 修改 `src/camels_switch_confirmation/run_parameter_path_mechanism.py`
- 修改 `test/test_camels_parameter_path_mechanism.py`

步骤：

1. 加入运行环境和 Python 可执行文件散列预检。
2. 固定 C、P 各 10 个任务；失败即停止新增提交。
3. 验证旧 03、03A 输出只读且内容散列不变。
4. 把预注册的 CPython、编译器、机器、NumPy 和双精度语义写成独立固定常量，拒绝登记值与实测环境共同漂移。
5. 新输出解析后只允许位于工作树的 CAMELS 结果根，且不得进入来源、旧 03 或旧 03A 证据根。
6. 在创建输出前核验独立初始矩清单的 SHA-256、流域—种子身份、形状、有限性、概率归一、协方差对称性和三类轴顺序。

## 任务 6：完整测试与单任务内存诊断

1. 运行路径算法、状态映射、切换运行器、机制运行器和独立核验器相关测试。
2. `git diff --check`。
3. 只在内存重放 03A 的两个失败任务，证明新合同不再把合法浮点重组差当作失败，同时两级硬门通过。
4. 任一测试或高精度门失败即停止，不创建配置。

## 任务 7：创建并独立复核 03B 配置

文件：

- 新建 `src/camels_switch_confirmation/configs/camels_parfc_transition_path_03b_mechanism10.json`
- 新建 03B 运行清单和只读预检证据。

步骤：

1. 登记最终预注册、代码、测试、Python 可执行文件、输入和旧失败证据散列。
2. 创建并登记独立初始矩清单，内容为 10 个流域—种子任务的初始概率、三个候选初始状态和协方差及轴元数据。
3. 使用全新结果根和日志名；确认均不存在且不位于任何既有证据根内。
4. 两名独立审查者给出只限 20 任务的 GO。

## 任务 8：唯一 20 任务运行和独立核验

1. 按运行清单逐字执行 C、P 各 10 个任务。
2. 运行独立核验器；保存原始结果、日志、核验表和集合散列。
3. 若技术门失败，最终 HOLD，且不再修订。
4. 若技术门通过，报告两项科学继续指标；不自动创建或运行 90 流域实验。
