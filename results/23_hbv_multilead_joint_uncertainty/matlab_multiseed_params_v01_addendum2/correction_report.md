# 更正补档 2：发表候选为均匀网格——参考对比表述全面改写（matlab_multiseed_params_v01_addendum2）

日期：2026-07-22。触发：第一层独立审查发现（严重①）。本补档取代第一份补档（matlab_multiseed_params_v01_addendum）中关于参考可交换性与"rng(1) 行为运行后加入"的推断表述；第一份补档的三个诊断**数字**均已被两层审查核实，继续有效。

## 新事实（审查发现，本上下文亲手复核）

1. **发表工作区的候选四参数全部是精确均匀网格** `linspace(下界, 上界, 10)`（与 linspace 最大偏差 ≤1.1e-13，升序含两端点）——不是任何版本 lhsdesign 的可能产出。跨码对齐轮已证明该网格候选正是产生发表 imm_probs 的候选（用存储候选逐循环复现 724/560）。
2. 网格含真值近邻：cw 611.1↔600、388.9↔400；cq 3.11↔3、5.89↔6。
3. **对照实验证实摊薄机制**（matlab_multiseed_grid_control_v01，12 个新种子 3002001–3002012，预测在跑前冻结于配置）：网格候选 + 随机噪声种子下，真滤波器阶段平均概率分布中位 0.253/0.373（LHS 24 种子为 0.471/0.553），发表参考 0.333/0.365 落回网格分布之内（阶段 1 经验分位 0.92、阶段 2 在四分位距内）；阶段 2 首登顶 120 = 网格分布最大值。删失率跳升（稳定登顶：阶段 1 删失 3/12、阶段 2 删失 5/12，LHS 下各 1/24）——真值近邻使稳定认出显著更难。

## 更正后的结论表述（取代 final_report.md 三个"异常"行与第一份补档的相关推断）

- **主交付物不变**：24 个 LHS 种子刻画的是"现行脚本文本设计点"的分布（稳定登顶中位 1030/1072 循环、每相删失 1/24、**≤10 循环认出比例 0/24**）。网格对照下同样 **0/12**。"10 循环预算内认不稳"跨全部 36 个新种子 + 发表实现成立。
- **参考对比改写**：发表实现来自候选生成机制不同的另一设计点（均匀网格），不得与 LHS 24 种子作同分布单种子解读。在正确的（网格）参考总体下，发表值 6 项中 5 项"非异常"；唯一残余是阶段 1 稳定登顶 724 快于全部 12 个网格种子（单侧 ~1/13 事件，样本小，不作机制解读）。原 final_report.md 三个"异常，单实现结论依赖种子"标签**作废**。
- **不可再生性细化**（取代第一份补档表述）：发表运行执行的不是任何现存脚本文本（候选=网格 vs 两版文本的 LHS；噪声档=cell1/1e-6 vs 文本的 cell2/1e-3；首批噪声不属于 rng(1) 流）。候选部分确定性、可精确再生；真值噪声实现不可由种子再生但已存档于 .mat。"rng(1) 行为运行后加入"只是候选解释之一，可证的表述是"发表运行 ≠ 现行脚本文本"。

## 对论文的修复建议（更新）

投稿包脚本与随附结果存在三处不一致：候选机制（LHS vs 实跑网格）、噪声档（1e-3 vs 实跑 1e-6）、随机实现（不可由 rng(1) 再生）。可行修复：(a) 按现行脚本 + rng(1) 重新生成合成实验并更新图表（本轮 24+12 种子分布表明定性结论跨种子、跨两种候选机制稳健，重生成风险低）；或 (b) 把实跑设定（网格候选 + 1e-6 噪声）写回脚本并在数据声明中注明"噪声实现以随附 .mat 为准"。

## 证据

- 网格验证数字：本文第 1 条（linspace 偏差表）；对照实验密封于 `matlab_multiseed_grid_control_v01/`（12 种子 .mat + 锚点表 + 冻结预测的判定分支）。
- 审查报告原文见 `matlab_multiseed_params_synthesis_v01/`。
