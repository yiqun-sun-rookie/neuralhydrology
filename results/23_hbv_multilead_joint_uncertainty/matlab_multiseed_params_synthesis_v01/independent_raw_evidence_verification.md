# 独立复算报告（第二层：从原始种子文件重算，先算后比）— matlab_multiseed_params_v01

（独立复算上下文原文存档，2026-07-22。先独立计算后打开被审汇总；未 import 仓库模块。）

## 总结论

**PASS，零不一致。** 自写锚点实现，从 24 个 seed .mat 原始数组 + 发表 .mat（SHA-256 先验证）独立算出全部六锚点、分位数、删失、rng(1) 三档标签与桥接比例；随后与被审 per_seed_anchors.csv（24 行全量）、anchor_distribution_summary.csv、summary.json、final_report.md 逐项全精度对照——144 个逐种子锚点值、30 个分位数、6 个 rng(1) 参考值、删失计数、三档判定、桥接比例全部一致（浮点 diff = 0）。两目录 checksums 全核（主 30/30、补档 6/6，无未列文件），config_snapshot 与 src 冻结配置逐字节相同。补档诊断 1/2 的全部 12 个对比数字从存档 .mat 位精确复现；诊断 3 属逻辑核查，其数字与推断自洽且有旁证。

## 事实（要点）

1. 种子完整性：seeds/ 恰 24 个文件 3001001–3001024，无缺失/多余/error；24/24 通过形状 (3649,12)、全有限、首行均匀、行和≈1、seed==文件名、t1_2=1216/t2_3=2433；抽查候选真值行全部吻合。
2. rng(1) 参考锚点从发表 .mat 独立算得 0.33259149687632206 / 6 / 724、0.3650905271830478 / 120 / 560，与 summary.json 全精度一致。
3. 逐种子 24×6 与 per_seed_anchors.csv 浮点差 0；删失位置一致（阶段 1 = 种子 3001013、阶段 2 = 种子 3001015，各 1 个不同种子）。
4. 分布汇总六行（min/q25/median/q75/max、删失、三档判定、分位注记 0.04、桥接 0/24）全部重算一致；三档规则应用与冻结规则逐项相符、无事后加档。
5. 补档：诊断 1/2 全部 12 个 max_abs_diff 从 seed1_mirror / seed1_variant .mat 位精确重算复现；诊断 3 逻辑核查成立（比值非常数变号 → 任何尺度下非同流；0.173 与两独立缩放 randn 块的量级预期一致）；旁证：发表 noise_q_truth_s1 逐列 std ≈ sqrt(1e-6·diag)，第 5 列恒 0 → 噪声"值"没漂，漂的是 RNG 状态。
6. rng(1) 3/6 指标落 [min,max] 外（p1_mean、p2_mean、p2_first），偏离方向全在"偏好更弱/认出更慢"一侧。

## 未知与限制

- 未重跑 MATLAB：seed .mat 是否忠实产自冻结驱动属第一层范围；本层只保证"从这些 .mat 出发的统计正确"。
- 诊断 3 的 randn 流重抽无法在 Python 复算（MATLAB ziggurat 流不可复制），该项为逻辑+量级核查。
- "经验分位位置 0.04"的分母口径（23 或 24）不可区分，但舍入后同值。

## 不一致明细

无。全部对照项零不一致。
