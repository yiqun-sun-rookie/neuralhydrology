# 独立审查报告（第一层：方法与结果）— walrus_imm_alignment_v02

（独立审查上下文原文存档，2026-07-22）

## 第一次

我按六项任务逐项审了设计文档、v01/v02 配置、walrus_port.py、31 个测试、运行器、被复用的 sigma_filter.py/imm.py、MATLAB 源脚本与 WALRUS 工具箱快照、官方 R 包，以及密封输出目录，并在 scratchpad 里跑了独立复算脚本。初步结论：

1. **判据冻结**：v01→v02 结构化 diff 证实只动了 G0 输入事实核对（q_s_all_rule 从 1e-3 改 1e-6 + 新增 noise_std_consistency）与 experiment 名/输出目录/amendment 块；g1/g2/g3 判据、容差、verdict_tiers、filter_settings 逐字节相同。修订理由用 .mat 独立证实：工作区 q_s_truth cell 1 = 1e-6·diag([140,1000,4,1200,0])，q_s_all 全 12 个逐位等于该 cell 1；noise_q_truth 逐列经验 std 与 sqrt(1e-6·diag) 之比为 0.989–1.005（若按脚本文本 cell 2=1e-3 应差约 31.6 倍，按 1e-5 应差 3.16 倍）——"脚本文本漂移"论断成立，且 for_submit 与仓库两份脚本文本一致漂移，"投稿包无法复现随附 .mat"这个复现性发现也成立。
2. **移植忠实性**：walrus_st_sp、statetran_fcn 及全部 7 个子函数逐行对照通过——包括 hq0/q0 入口钳位、q 用新 hs 在 pond_flood 之前求值、pond_flood 四个 if 顺序执行（非 elif）语义、`if hs<-0.001 / elseif hq<-0.001` 守卫、ets 的 `hs < min_h*1e3` 守卫、pq_pv_ps 默认 ag=0.99。31/31 测试通过；dveq 测试的 oracle 与官方 R 包 WALRUS_defaults.R 原文逐项一致，且与 MATLAB 代数形符号上不同，oracle 独立性成立。
3. **运行器与 MATLAB 语义映射**：四道门顺序、G0 失败中止、G1 失败跳过 G2/G3、判定分级严格按冻结 tiers 实现。索引映射（predict 用 p[k−1]/etpot[k−1]、correct 用 qobs[k]、噪声取第 k 行）与 MATLAB 1-based 循环一一对应；转移矩阵 0.99 对角 + 0.01/11 非对角符合 trackingIMM 标量语义；初始概率均匀 1/12（npz 第 0 行与 .mat 参考逐位相等）；measure=state(end)、r=1.5e-5、UT α/β/κ 全对；IMM 概率更新（混合→子滤波→posterior ∝ c_j·Λ_j）与 trackingIMM 标准语义一致。
4. **数值抽查**：从密封 g3_prob_diff.npz 重算 max|Δprob| = 1.584074429097855e-02，与 gate_results.json 一致到最后一位；用我自己写的锚点代码重算六锚点：phase1 均值 0.3325914969/0.3325217321、first-top 6/6、stable-top 724/724；phase2 0.3650905272/0.3672011560、120/120、560/560——与 CSV 及声称完全一致。npz 里 reference_probs 与 .mat imm_probs 逐位相等（密封件确实钉在参考上）。g1_replay_diff.npz 最大值 4.547473508864641e-13；我从 .mat + walrus_port 独立重放 3649 步得到完全相同的逐状态最大差。checksums.json 覆盖全部 9 个文件（除自身），全部匹配；config_snapshot.json 的 SHA 等于 v02 配置（2b2251…）；.mat SHA 等于 fd080c…。
5. **G2 诊断**：我重跑 filter 1 前 30 步，first_exceed=20 复现；超差分量正是 hq（差 2.45e-6 > 1e-6 绝对容差），参考 hq(20)=8.24e-4、hq(21)=7.40e-4（"≈7e-4 近零"属实）；且 sigma 点 hq 区间从第 18 步起跨零（[−7.98e-4, 4.68e-3]），第 19 步差值从 2.5e-9 跳到 1.8e-5 恰在跨零开始——诊断"近零钳位区放大数值差"成立且机制明确（max(hq,0) 非光滑分支 × 双方 sigma 点集只保证二阶矩相同）。
6. **限制**：G2 的 Python 轨迹确实没进密封目录，只有汇总 CSV。

初步判定：通过，"结构等价"结论成立。

## 自评

把第一次当别人的报告重读，挑出这些问题：

- 我差点把"数值全对"当成"流程可信"。**全链条（设计文档、两份配置、移植代码、测试、运行器、密封结果）都没有 git 提交**，"先冻结后看数"目前只有三条较弱的证据链：v01/v02 diff 只动 G0、gate_results 内嵌 config SHA、mtime 链（v01 15:10:58 → v02 15:20:25 → 设计修订 15:21:12 → 运行 15:21:41 → 密封 15:24:34，本地时间）。mtime 可改，设计文档自身的 SHA 没被任何密封工件钉住。这必须升为"重要"问题而不是一句带过。缓解因素是真实的：报告的是中间档"结构等价"而非最讨好的"数值一致"，G2 失败被如实报告、容差未放宽——造假动机下不会是这个形状；但审计性缺口仍在。
- "修订时 G2/G3 比对从未执行过"这句话我无法从工件证实（只能证明不存在 v01 密封目录），应归入"未知"而非默认采信。
- 第一次没有点破：设计承诺 G2 超差时"报告首个分歧步的逐中间量诊断（sigma 点、先验、增益）"，密封目录里**没有**这个工件——hq≈7e-4 的诊断只存在于声称/笔记里，是我自己复算才坐实的。承诺与交付有缺口。
- "结构等价"用词：档位是预冻结的、条件全满足，用词合法；但第一次没写清证据边界——这是**单一设计点、单一噪声实现**上的等价，且 G2 强判据（1e-6 绝对容差打在 ~1e-3 量级的 hq 上，叠加预登记的 Cholesky 平方根差）本来就几乎不可能过，失败不构成 bug 证据。反过来，第 2–17 步双方差 ≤2.6e-9 是结构语义（加性噪声位置、correct 端重生成 sigma、权重、似然）高度吻合的正面证据——语义级错位会在第 2 步就出现远大于此的差。
- 补一个第一次漏掉的核查：max|Δprob|=0.0158 出现在第 706 步 filter 2（参考 0.0335 vs Python 0.0176），是低概率非真滤波器的瞬态，不影响锚点——这支持"分歧集中在无关紧要处"的解读。
- 轻微不一致：设计/配置写"四个锚点"，实际是 3 指标×2 阶段=6 个值，代码检查全部 6 个（更严，无风险）；配置列了 states_pred_filters 但 runner 未用；G2 容差 1e-6 硬编码在 runner 而非读配置；"终值最大差 0.14–0.77"无量级参照（状态量级 200–2450，相对约 0.03–0.4%），且 0.1366 被写成 0.14。

自评后结论不翻转，但"通过"要挂上明确整改项。

## 最终

### 事实（已独立验证）

- 哈希：.mat = fd080c…、v01 = 34f6d9…、v02 = 2b2251…、密封 config_snapshot ≡ v02，全部与声称一致。
- v01→v02 只改了 G0 输入事实核对；G1–G3 判据、容差、判定分级逐字节未动。
- 漂移论断成立：工作区 q_s_truth={1e-6,1e-3,1e-4}·diag、q_s_all 全 12 个逐位 = 1e-6·diag；noise_q_truth 经验 std/sqrt(1e-6·diag) = 0.989–1.005；仓库与 for_submit 脚本文本均写 {1e-5,1e-3,1e-4} 且取 cell 2 → 照文本跑过程噪声方差大 1000 倍，复现性缺陷发现成立。
- 移植逐函数忠实（含全部守卫顺序与 q-before-pond_flood 细节）；31/31 测试过；dveq oracle 与官方 R 包原文一致且与 MATLAB 形式符号不同，独立性成立。
- G1 = 4.547473508864641e-13，我从 .mat 独立重放得到逐位相同的结果。
- G2：first_exceed 分布 20–219、终值最大差 0.1366–0.7715（相对状态量级约 0.03–0.4%），与密封 CSV 一致；filter 1 第 20 步超差在 hq 分量（2.45e-6 对 1e-6 绝对容差），hq≈8.2e-4，sigma 点自第 18 步跨零——"近零钳位区"诊断成立。
- G3：max|Δprob| 与六锚点全部复算一致，锚点全部在容差内；最大概率差在第 706 步的低概率滤波器上；初始概率行与 MATLAB 逐位相同。
- checksums.json 覆盖除自身外全部 9 个文件且全部匹配；runner 的判定分级实现与冻结 tiers 一致。

### 推断

- "结构等价"档位使用合法（预冻结、条件满足）。合理解读是：**在该设计点上，Python 滤波层与 MATLAB 无可检出的结构性/语义性差异；残差与预登记的"sigma 点平方根实现差 × 非光滑钳位放大"机制一致**。第 2–17 步 ≤2.6e-9 的吻合度是语义正确的正面证据。它不是"两实现处处等价"的断言，正式引用时应带"单设计点、单噪声实现"限定。
- G2 强判据在预登记的合法分歧源存在下本就几乎不可达，未过不构成缺陷证据。
- mtime 链与工件内 SHA 交叉引用与"先冻结后看数"的叙述一致，未发现任何事后调判据的迹象（最有力的旁证：结论停在中间档且 G2 失败如实报告）。

### 未知

- "修订时 G2/G3 从未执行过"无法从工件证实（仅能确认不存在 v01 密封目录）。
- MATLAB 端内部细节（trackingUKF correct 是否重生成 sigma、chol 方向）没有 MATLAB 运行可直接验证——只能由步 2–17 的吻合度间接支持。
- 设计文档本身内容在运行后是否被改动，因未入 git 无法排除。

### 问题清单

| 级别 | 问题 | 处置建议 |
|---|---|---|
| 严重 | 无 | — |
| 重要 | ① 全链条未提交 git（设计、配置、代码、测试、密封结果全 untracked），"预冻结"目前不可独立审计 | 立即一次性 commit 全部工件，使 SHA 链落地 |
| 重要 | ② 设计承诺的 G2 首个分歧步逐中间量诊断（sigma/先验/增益）未进入密封目录，hq≈7e-4 诊断无密封证据（本审查已独立复算坐实） | 以 addendum 目录补诊断工件，不动已密封 v02 |
| 重要 | ③ G2 Python 轨迹未密封（仅 ~1.75 MB），二层复验必须依赖仓库代码重跑而非密封工件 | 同上，addendum 存 12×3649×5 轨迹 npz + checksums |
| 轻微 | ④ "四个锚点"措辞 vs 实际 6 个值（代码查 6 个，更严） | 文字修正即可 |
| 轻微 | ⑤ 配置列 states_pred_filters 但未加载使用 | 从配置移除或注明 |
| 轻微 | ⑥ G2 容差 1e-6 硬编码于 runner（与配置一致但绕过单一来源） | 改为读配置 |
| 轻微 | ⑦ "终值最大差 0.14–0.77"无量级参照（相对仅 0.03–0.4%），0.1366 写作 0.14 | 报告中补相对量级 |
| 轻微 | ⑧ scipy loadmat duplicate-variable 警告（MATLAB 函数句柄工作区），无实际影响 | 无需处理 |

### 结论

**通过（附整改）**。四道门的数值结果、G0 修订的事实依据、移植忠实性、MATLAB 语义映射、六锚点复算、checksums 完整性全部经独立核验成立；"结构等价"判定在预冻结分级内、未超出证据。三项"重要"问题全部是过程审计性缺口（未入 git、G2 诊断与轨迹未密封），不动摇科学结论，但在入 git + addendum 补齐之前，该结果不应被当作"已通过完整两层审计"引用。
