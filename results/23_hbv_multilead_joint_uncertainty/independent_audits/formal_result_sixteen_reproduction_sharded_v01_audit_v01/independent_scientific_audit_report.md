# 正式逐流域复现独立科学与数值审查

## 结论

**RELEASE**

正式逐流域复现可以放行。阻断问题数为 0，非阻断问题数为 0。

## 可验证结果

- 流域身份：正式清单包含 16 个不同流域；会话、16 个逐流域结果、汇总结果、合同和原正式结果的流域及顺序一致。
- 原始数组：每个流域 36 个数组、366,960 个数值；总计 576 个数组、5,871,360 个数值。逐流域结果、汇总结果和原正式结果三方最大绝对差为 `0.0`，16 份压缩数组文件均逐字节相同。
- 独立复算：重新计算 240 行逐流域指标、15 行跨流域汇总、18 行配对比较、816 行候选模型概率、288 行参数与噪声边际概率。与正式表格相比的最大绝对差不超过 `5.684341886080802e-14`；该差值来自十进制表格解析，正式汇总结果与原正式结果的对应文件校验值完全相同。
- 概率约束：240 项概率和检查全部通过；候选模型概率最大差为 `1.1102230246251565e-16`，参数与噪声边际概率最大差为 `1.1102230246251565e-16`。
- 图和报告：2 项图像比较均逐像素相同，2 项报告比较均逐字节相同。
- 内容闭合：36/36 个产物内容清单通过；18/18 条注册记录均从“运行中”闭合到“已验证”；16/16 份评价期输入审查通过。
- 排除失败结果：发现 16 个声明成功且含逐流域清单的目录；失败或未完成目录数为 0；被汇总的失败或未完成目录数为 0。

## 不同预报时长的结果

这里比较“联合参数与噪声不确定性的交互式多模型滤波”与“固定参数、固定噪声的单一滤波”。均方根误差变化为负数表示联合方法更低。

| 预报时长 | 均方根误差中位数变化 | 16 个流域中的胜出数 | 纳什效率系数中位数差 |
|---:|---:|---:|---:|
| 1 天 | -10.17189495693546% | 10 | +0.022635420309362053 |
| 3 天 | -2.560778471380311% | 10 | +0.020485694739211247 |
| 7 天 | -4.542489566451767% | 9 | +0.0437261883261541 |

## 固定身份

- 合同 `checksums.json` 的 SHA-256 文件摘要（一种用于识别文件内容是否变化的校验值）：`c7615c868b747d1b633eb5d32432376b5648087846d97d0ff5fa507ecdf4c357`。
- 原正式结果 `checksums.json` 的文件摘要：`88ab2297e02179cc0e3e432c958871e1d6e7985358b9394970154a65f2348bd0`。
- 正式复现会话 `checksums.json` 的文件摘要：`86c3232fca046b10cfcc269c96b08a8d5ed2a59d80bf91f6b37c2892f089734b`。
- 固定源代码 37 个文件、固定输入 62 个文件、控制文件 3 个；内容不一致数为 0。

## 资源与完整日志

- 本次正式复现链含 18 次运行：1 次会话建立、16 次逐流域计算、1 次汇总。18/18 份完整日志通过，共 1,203 个采样点；运行期间最低可用内存比例为 `0.21666502607813742`，最大监测间隔为 `9.93487` 秒，最低磁盘可用空间为 `2166.1255378723145` 吉字节，最大进程常驻内存为 `0.17947769165039062` 吉字节；数值计算线程均限制为 1。
- 连同合同生成和原正式结果生成，共审查 20 次运行、2,497 个采样点，20/20 通过。
- 合同结果目录中的 137 点资源文件和原正式结果目录中的 968 点资源文件，分别精确等于完整日志的前缀，并止于 `before_contract_write` 和 `before_result_write`。规范日志分别含 232 点和 1,062 点，均从 `start` 到 `finish`。
- 两份规范日志的路径由各自产物配置中的日志根目录和产物标识唯一确定；运行摘要同时绑定产物目录、产物标识、样本数、末事件及资源日志文件摘要。两份绑定的 17 项检查全部通过。

## 审查器误判复验

第二次审查曾把结果目录中的写盘前快照当作完整日志，产生 2 项错误阻断。该次输出已原样保留在名称含 `attempt02_resource_misclassification` 的文件中。修正后独立验证了快照前缀关系、规范日志文件摘要、运行摘要绑定、起止事件、资源阈值和执行签名；第三次运行退出码为 0，最终问题数为 0。

第一次运行因审查代码没有区分数值数组与文字数组而退出；对应输出已保留在名称含 `attempt01_script_error` 的文件中。该失败只发生在新建的独立审查器，不改变被审查产物。

## 独立性边界

指标、汇总、配对比较、候选模型概率、参数与噪声边际概率、图和报告均由审查目录内的独立脚本重新实现；该脚本不导入项目的计算或验证模块。审查只读取目标产物，所有新增文件均位于本审查目录。

