# 最终综合证据第一次独立只读审查

## 结论

**BLOCK。正式十六流域端到端复现仍为`PENDING`，本审查没有把它判为通过。**

正式数组中的核心数值、流域内成对定义、参数候选规则、过程噪声公式、起报日后验修正、三个状态更新参照和不更新状态原模型结论均重算一致。报告也正确地区分了“各方法跨流域中位数之差”和“先在同一流域求相对变化再取中位数”；十二组比较中有四组符号相反，最终报告用于方法判断的都是后者。

阻断发布的问题为2项；另有1项不阻断的证据追踪问题。

## 阻断问题

- I001：把十六个正式评价流域称为“十六个未见流域”。
  - 证据：16/16正式流域都在冻结的531流域训练参数投影中；每个流域中心参数使用1999-10-01至2008-09-30资料训练。它们只是不属于六流域开发集合，不能称为未见流域或无资料流域。
  - 必须修改：改为“十六个未参与六流域开发迭代的正式评价流域”或同等精确表述。
- I002：报告称观测噪声对五种方法固定一致，但不更新状态原模型不读取流量、也不使用观测噪声。
  - 证据：冻结runner.py先单独运行不更新状态原模型，随后只对METHOD_ORDER[1:]的四种状态更新方法构造含观测协方差的滤波器。
  - 必须修改：改为“四种状态更新方法使用同一观测噪声；不更新状态原模型不使用观测噪声”。

## 不阻断问题

- I003：核验说明引用正式结果独立审查的候选数与映射，但证据路径没有列出该审查；现有interaction_audit.json主要记录六个开发流域的交互模式选择。
  - 必须修改：在C01证据路径加入../independent_audits/formal_result_sixteen_v01_v02/array_and_table_audit_summary.json，或加入等价的正式九候选映射证据。

## 独立重算的关键结果

| 预见期 | 联合方法相对仅过程噪声变化方法：流域内成对均方根误差中位变化 | 胜出流域 | 联合方法相对不更新状态原模型：流域内成对均方根误差中位变化 | 胜出流域 |
|---:|---:|---:|---:|---:|
| 1天 | -0.261220% | 11/16 | +0.175180% | 8/16 |
| 3天 | +0.141942% | 8/16 | +40.612665% | 5/16 |
| 7天 | +0.888996% | 8/16 | +88.529996% | 4/16 |

联合方法相对固定状态更新器的三个成对中位变化为-10.171895%、-2.560778%、-4.542490%；相对仅参数变化方法为-3.654503%、-4.401634%、-6.219162%。这些数值均从十六个压缩数组重新计算，不读取正式汇总表作为数值输入。

起报日后验直接逆解得到5,728点：参数因素和过程噪声因素均在16/16流域至少切换一次；总变差距离中位数为0.002034747，大于0.05为723/5,728。

## 定义与边界

- 参数候选：每流域1,024个冻结候选记录；十六个合同的最大最小距离候选对全部重建一致。三十二个非中心向量中，二十三个开发期效率较低，九个较高。
- 过程噪声：十五维对角线的前五项公式逐行重算，最大绝对差不超过0.000e+00；后十项全部为0。
- 起报后资料：黑箱替换未来流量的45个预测值最大变化为0；未来历史实测气象仍使本试验只能解释为诊断回报。
- 不更新状态原模型：它不使用流量更新，也不使用观测噪声；观测噪声只在其余四种状态更新方法中固定一致。
- 正式复现：完整目录不存在，仅保留四个已完成流域和291条资源样本；最低可用内存17.916238%，因此状态只能是`PENDING`。
- 已知问题表：十三项逐条复核，十三项的成立、不成立或边界状态均与冻结证据一致。

## 完整性和只读性

正式合同、正式结果、六流域结果和三个正式结果审查包的登记校验和均通过；Kalmannet证据包自身清单和二十个外部源文件当前校验和均通过。所有相对链接和证据路径均存在。受审目录在本次审查前后将由`package_integrity_before_after.json`逐目录比较。
