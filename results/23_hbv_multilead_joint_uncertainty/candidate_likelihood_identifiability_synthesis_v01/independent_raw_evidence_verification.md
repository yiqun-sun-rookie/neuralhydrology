# 十五状态候选观测似然诊断：独立原始证据复算

## 结论

原始证据复算未发现结论性缺口，不触发暂缓。三种场景的第二、第三阶段全部未通过冻结门槛，明确不支持“真实候选在切换后十个评分日内稳定超过错误候选”。

本次复算没有读取确认目录的 `summary.json`、`scientific_evaluation.json` 或 `stage_identifiability.csv`，也没有使用先前审查正文。输入仅包括冻结配置、`evidence.npz`、逐候选逐日表、试验阶段表、源数据快照、目录清单和保护文件记录。

## 公式与数组复算

标量高斯预测观测对数似然按下式独立复算：

`-0.5 * (log(2 * pi * innovation_variance) + innovation^2 / innovation_variance)`

- 观测重构最大绝对误差：0。
- 创新恒等式最大绝对误差：0。
- 逐日观测对数似然最大绝对误差：`7.105427357601002e-15`。
- 第六日至第十五日评分、阶段内累计、并列不利排名、逐日和累计真实候选差、首次第一、首次第一且保持、最后五日稳定标志：与保存数组逐元素一致。

## 六个切换后阶段

| 场景 | 阶段 | 稳定数/总数 | 稳定比例 | 十二区块、二万次重采样 95% 区间 | 第十日累计差中位数 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| 完整参数向量切换 | 2 | 8/36 | 0.2222222222 | [0.1388888889, 0.3055555556] | -0.1629359204 | 不通过 |
| 完整参数向量切换 | 3 | 13/36 | 0.3611111111 | [0.2777777778, 0.4444444444] | -0.0175998312 | 不通过 |
| 过程噪声切换 | 2 | 12/36 | 0.3333333333 | [0.1944444444, 0.4722222222] | -0.3216476585 | 不通过 |
| 过程噪声切换 | 3 | 13/36 | 0.3611111111 | [0.1944444444, 0.5277777778] | +0.1529064441 | 不通过 |
| 完整参数向量与过程噪声联合切换 | 2 | 7/108 | 0.0648148148 | [0.0370370370, 0.0925925926] | -0.5624520180 | 不通过 |
| 完整参数向量与过程噪声联合切换 | 3 | 12/108 | 0.1111111111 | [0.0740740741, 0.1574074074] | -0.3119350238 | 不通过 |

## 逐行表复算

- 逐候选逐日表共 53,460 行：联合场景 43,740 行，另两个场景各 4,860 行。键集合完整且无重复；类别、标志、阶段、评分日和真实候选字段不一致数均为 0。
- 逐行浮点最大误差：预测观测 `1.7763568394002505e-15`，创新 `8.881784197001252e-16`，创新方差 `8.881784197001252e-16`，逐日对数似然 `7.105427357601002e-15`，阶段累计 `2.842170943040401e-14`。
- 试验阶段表共 540 行：联合场景 324 行，另两个场景各 108 行。排名、首次日期、稳定标志和候选标识不一致数均为 0；最终累计差最大误差为 `1.7763568394002505e-15`。
- 参数、过程噪声协方差和观测误差快照与原始数组的最大误差均为 0；正式源文件的安全散列算法校验值与配置声明一致。

## 封存与配置复算

- 使用 Windows 扩展长度路径复核，每个目录的 `checksums.json` 覆盖 30 项；目录实际 31 项，额外项仅为清单自身。缺失、多余和校验值不符均为 0。
- 每份确认配置的 59 个保护路径展开为 738 个文件。三个运行各自的运行前、写入后和当前集合完全相同，738 项校验值不符数均为 0。
- 每次运行前保护映射均同时包含三份确认配置；运行前、运行后和当前配置校验值一致。
- 配置快照与当前冻结配置逐字节一致；声明的第五版开发配置校验值全部复算一致。
- 剔除获准变化的确认身份、实验名称、区块与三类合成数据种子、目的、范围和保护元数据后，确认与开发科学设置的意外差异为 0。
- 确认数据种子与第五版开发、第二版正式数据种子按气象、过程噪声和观测噪声分别求交均为空集。区块重采样种子与开发相同，这是冻结统计规则，不是合成数据种子。

## 边界

证据链支持的是“在当前冻结条件下未达到稳定可辨识标准”，而不是“所有条件下均不可辨识”。没有运行、修改或覆盖任何开发、确认或正式实验。
