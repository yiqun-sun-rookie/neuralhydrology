# G2 轮补遗（addendum-01）：审查修正项 — 2026-07-22

本补遗响应第一层独立审查的三项必须修正（均不改动任何密封文件与冻结设计文档）。

## 1. 轮次叙事数字更正

- 更正前（错误）："通过格中位 T 12–36 循环；失败格删失 33–60%"
- 更正后（以密封 curve_summary.json 为准）：
  - **8 个通过格** pooled 中位 T 范围 **10.0–36.0** 循环（最小 m8/L45=10.0，最大 m2/L180=36.0）
  - "12.5 / 19 / 36" 是**各行边界最小通过格**（m4/L45、m2/L90、m2/L180）这一子集的中位 T，引用时必须写明子集
  - **12 个失败格** pooled 删失比例范围 **29.2%–59.7%**（最小 m2/L45，最大 m05/L015）

## 2. 第三条解释边界（补登记）

设计文档第 1 节预登记了两条解释边界（倍率同时改变真值轨迹；预算轴拉长季节周期），漏登记了作用在主交付物另一根轴上的第三条，现补：

> **T 的后缀定义使跨 L 比较含窗长机械成分。** `first_stable_cumulative_rank_one_day` 要求累计排名从该评分日起保持第 1 **直到阶段末**。评分窗越长，"保持到末尾"的绝对天数要求越长，阶段中段任何一次被反超都会把 T 机械推后。因此 m=0.5 列中位 T 随 L 上升（11→18→22→62）不能全部读作"预算越大认出越慢"。逐格三件套判定不受影响（`latest_qualifying_day = 评分日−5+1` 随 L 放宽是总目标"时间长一点认出也是认出"的有意设计）；受影响的只是 T 数值的跨行解读。

## 3. L=180 行的准确表述（规则允许的部分区间）

冻结规则要求矛盾行"只给区间"。准确表述为：

- m=2：开发与确认两组独立种子**均通过**三件套 → 通过边界上界 ≤ 2.0 有两组种子支持
- m=1：开发 fail（阶段 2 三项判据全不达标）、确认 PASS → **种子依赖，边缘格**
- 行结论：通过边界位于 m ≤ 2.0 区域，其确切位置（是否低至 1.0 或更低）目前无法确定；按冻结规则不重跑，此为最终结论

## 4. 记录项（无需行动）

- 开发曲线 final_report.md 显示层用 `:.0f`（银行家舍入）使 4 格中位显示值比 JSON 精确值少 0.5（12.5→12、18.5→18、60.5→60、62.5→62）；JSON 与 CSV 保留精确值，判定不受影响
- m=1 缩放 CSV 因 `center+(v−center)` 浮点重构，parTT 与源值差 −8.3e-17（1 ulp），非 bit 级复用；科学上可忽略
- 配对设计（行内 5 倍率共用区块种子）下，"倍率轴单调零违背"的证据力应按配对设计折减理解（行内 5 格非独立样本）；跨行无此问题
- 第二层复算审查记录的 5 条定义理解歧义（bootstrap 阶段种子偏移 +1/+2、真候选取值日、rank 含自身、稳定门槛边界、删失分位数规则）已随其报告存档
