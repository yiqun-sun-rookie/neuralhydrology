# NeuralHydrology CPU版本依赖包
# 适用于没有GPU或使用CPU训练的用户

# 核心科学计算包
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
xarray>=0.20.0

# 深度学习框架 (CPU版本)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 数据处理和可视化
matplotlib>=3.5.0
h5py>=3.6.0
netcdf4>=1.6.0

# 配置和工具
ruamel.yaml>=0.17.0
tqdm>=4.64.0
tensorboard>=2.8.0

# 性能优化
numba>=0.56.0

# 安装说明：
# 1. 使用以下命令安装CPU版本的PyTorch：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# 2. 然后安装其他依赖：
#    pip install -r requirements-cpu.txt
